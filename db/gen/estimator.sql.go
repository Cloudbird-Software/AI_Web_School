// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: estimator.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getActiveEstimatorRun = `-- name: GetActiveEstimatorRun :one
SELECT run_id, purpose_scope, model_version, code_digest, input_snapshot_id, graph_release_id, activated_at, retired_at, created_at, activated_by FROM estimator_run
WHERE purpose_scope = $1 AND retired_at IS NULL
ORDER BY activated_at DESC LIMIT 1
`

// 当前活跃指针（retired_at IS NULL 即唯一活口——偏唯一索引的谓词面）。
func (q *Queries) GetActiveEstimatorRun(ctx context.Context, purposeScope string) (EstimatorRun, error) {
	row := q.db.QueryRow(ctx, getActiveEstimatorRun, purposeScope)
	var i EstimatorRun
	err := row.Scan(
		&i.RunID,
		&i.PurposeScope,
		&i.ModelVersion,
		&i.CodeDigest,
		&i.InputSnapshotID,
		&i.GraphReleaseID,
		&i.ActivatedAt,
		&i.RetiredAt,
		&i.CreatedAt,
		&i.ActivatedBy,
	)
	return i, err
}

const getActiveEstimatorRunAsOf = `-- name: GetActiveEstimatorRunAsOf :one
SELECT run_id, purpose_scope, model_version, code_digest, input_snapshot_id, graph_release_id, activated_at, retired_at, created_at, activated_by FROM estimator_run
WHERE purpose_scope = $1 AND activated_at <= $2
  AND (retired_at IS NULL OR retired_at > $2)
ORDER BY activated_at DESC LIMIT 1
`

type GetActiveEstimatorRunAsOfParams struct {
	PurposeScope string
	ActivatedAt  pgtype.Timestamptz
}

// D6 时间回溯：asOf 当时正活跃的版本（已登记且未退役或退役晚于 asOf）。
func (q *Queries) GetActiveEstimatorRunAsOf(ctx context.Context, arg GetActiveEstimatorRunAsOfParams) (EstimatorRun, error) {
	row := q.db.QueryRow(ctx, getActiveEstimatorRunAsOf, arg.PurposeScope, arg.ActivatedAt)
	var i EstimatorRun
	err := row.Scan(
		&i.RunID,
		&i.PurposeScope,
		&i.ModelVersion,
		&i.CodeDigest,
		&i.InputSnapshotID,
		&i.GraphReleaseID,
		&i.ActivatedAt,
		&i.RetiredAt,
		&i.CreatedAt,
		&i.ActivatedBy,
	)
	return i, err
}

const insertEstimatorRun = `-- name: InsertEstimatorRun :exec
INSERT INTO estimator_run (
	run_id, purpose_scope, model_version, code_digest,
	input_snapshot_id, graph_release_id, activated_by, activated_at
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
`

type InsertEstimatorRunParams struct {
	RunID           string
	PurposeScope    string
	ModelVersion    string
	CodeDigest      string
	InputSnapshotID string
	GraphReleaseID  string
	ActivatedBy     string
	ActivatedAt     pgtype.Timestamptz
}

// 后登记新活跃行（retired_at 缺省 NULL）；activated_by 为留痕的操作者维度。
func (q *Queries) InsertEstimatorRun(ctx context.Context, arg InsertEstimatorRunParams) error {
	_, err := q.db.Exec(ctx, insertEstimatorRun,
		arg.RunID,
		arg.PurposeScope,
		arg.ModelVersion,
		arg.CodeDigest,
		arg.InputSnapshotID,
		arg.GraphReleaseID,
		arg.ActivatedBy,
		arg.ActivatedAt,
	)
	return err
}

const listEstimatorRunLineage = `-- name: ListEstimatorRunLineage :many
SELECT run_id, purpose_scope, model_version, code_digest, input_snapshot_id, graph_release_id, activated_at, retired_at, created_at, activated_by FROM estimator_run
WHERE purpose_scope = $1
ORDER BY activated_at ASC
`

// 全量行账（升序）：前驱行 model_version 即 from，本行 who/when/to 取
// activated_by / activated_at / model_version——append-only 时间线还原面。
func (q *Queries) ListEstimatorRunLineage(ctx context.Context, purposeScope string) ([]EstimatorRun, error) {
	rows, err := q.db.Query(ctx, listEstimatorRunLineage, purposeScope)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []EstimatorRun
	for rows.Next() {
		var i EstimatorRun
		if err := rows.Scan(
			&i.RunID,
			&i.PurposeScope,
			&i.ModelVersion,
			&i.CodeDigest,
			&i.InputSnapshotID,
			&i.GraphReleaseID,
			&i.ActivatedAt,
			&i.RetiredAt,
			&i.CreatedAt,
			&i.ActivatedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockEstimatorScope = `-- name: LockEstimatorScope :exec

SELECT pg_advisory_xact_lock(hashtextextended('estimator_run:' || $1::text, 0))
`

// T-W5-019（SQL-2）：估计器活跃指针的读改写语句面。
// 切换临界区 = 调用方显式事务内的完整序列（D11 领域服务不自 commit）：
//
//	LockEstimatorScope（per-scope advisory xact lock，串行化含「首次登记无行可锁」
//	在内的全部并发切换者）→ GetActive*（幂等判定）→ RetireActive*（先退役腾出
//	偏唯一索引谓词槽位 retired_at IS NULL）→ InsertEstimatorRun（后插入不撞
//	uq_estimator_run_one_active_per_scope；activated_by 即 0025 留痕「谁」列）。
//
// 唯一索引是最后一道防线：其拒绝以 SQLSTATE 23505 到达应用层并被翻译为明确错误。
// 行锁备选（SELECT ... FOR UPDATE 对活跃行加锁）因覆盖不了空表首插竞态而未采用，
// 评审对照见 core/estimator/pg.go 注释。
// per-scope 事务级 advisory 排他锁（pg_advisory_xact_lock 返回 void，事务结束自动释放）。
func (q *Queries) LockEstimatorScope(ctx context.Context, dollar_1 string) error {
	_, err := q.db.Exec(ctx, lockEstimatorScope, dollar_1)
	return err
}

const retireActiveEstimatorRun = `-- name: RetireActiveEstimatorRun :exec
UPDATE estimator_run SET retired_at = $2
WHERE purpose_scope = $1 AND retired_at IS NULL
`

type RetireActiveEstimatorRunParams struct {
	PurposeScope string
	RetiredAt    pgtype.Timestamptz
}

// 先退役旧活跃行：先 UPDATE 后 INSERT 保证偏唯一索引不冲突（与冻结实现同序）。
func (q *Queries) RetireActiveEstimatorRun(ctx context.Context, arg RetireActiveEstimatorRunParams) error {
	_, err := q.db.Exec(ctx, retireActiveEstimatorRun, arg.PurposeScope, arg.RetiredAt)
	return err
}
