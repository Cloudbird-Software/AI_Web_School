// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: consent.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getLatestConsentEvent = `-- name: GetLatestConsentEvent :one
SELECT consent_id, student_alias_id, event_type, scope, valid_from, valid_until, version, created_at, recorded_by FROM parental_consent
WHERE student_alias_id = $1 AND scope ->> 'purpose' = $2::text
ORDER BY version DESC
LIMIT 1
`

type GetLatestConsentEventParams struct {
	StudentAliasID pgtype.UUID
	Column2        string
}

// 链顶事件（版本最大恰一行——0027 唯一索引保证确定性）。
// $2 显式 ::text 定型：scope ->> 'purpose' 的左值是 text，无定型时生成层会把
// 形参推成 jsonb 字节面（[]byte），运行时撞 text = bytea 无算符错误。
func (q *Queries) GetLatestConsentEvent(ctx context.Context, arg GetLatestConsentEventParams) (ParentalConsent, error) {
	row := q.db.QueryRow(ctx, getLatestConsentEvent, arg.StudentAliasID, arg.Column2)
	var i ParentalConsent
	err := row.Scan(
		&i.ConsentID,
		&i.StudentAliasID,
		&i.EventType,
		&i.Scope,
		&i.ValidFrom,
		&i.ValidUntil,
		&i.Version,
		&i.CreatedAt,
		&i.RecordedBy,
	)
	return i, err
}

const insertConsentEvent = `-- name: InsertConsentEvent :exec
INSERT INTO parental_consent (
	consent_id, student_alias_id, event_type, scope,
	valid_from, valid_until, version, recorded_by, created_at
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
`

type InsertConsentEventParams struct {
	ConsentID      pgtype.UUID
	StudentAliasID pgtype.UUID
	EventType      string
	Scope          []byte
	ValidFrom      pgtype.Timestamptz
	ValidUntil     pgtype.Timestamptz
	Version        int32
	RecordedBy     string
	CreatedAt      pgtype.Timestamptz
}

// 追加授权事件（grant/revoke 共面）：version 由调用方在锁内算出；
// created_at 显式传值使留痕时间线与内存实现逐字段同构；
// recorded_by 为留痕的登记主体维度（0027 列）。
func (q *Queries) InsertConsentEvent(ctx context.Context, arg InsertConsentEventParams) error {
	_, err := q.db.Exec(ctx, insertConsentEvent,
		arg.ConsentID,
		arg.StudentAliasID,
		arg.EventType,
		arg.Scope,
		arg.ValidFrom,
		arg.ValidUntil,
		arg.Version,
		arg.RecordedBy,
		arg.CreatedAt,
	)
	return err
}

const listConsentHistory = `-- name: ListConsentHistory :many
SELECT consent_id, student_alias_id, event_type, scope, valid_from, valid_until, version, created_at, recorded_by FROM parental_consent
WHERE student_alias_id = $1 AND scope ->> 'purpose' = $2::text
ORDER BY version ASC
`

type ListConsentHistoryParams struct {
	StudentAliasID pgtype.UUID
	Column2        string
}

// 全量事件账（升序）：第 n 行 version 即 from→to 时间线的 to(n)/from(n+1)，
// who/when 取 recorded_by / created_at——append-only 只读投影还原面。
// $2 ::text 定型理由同 GetLatestConsentEvent。
func (q *Queries) ListConsentHistory(ctx context.Context, arg ListConsentHistoryParams) ([]ParentalConsent, error) {
	rows, err := q.db.Query(ctx, listConsentHistory, arg.StudentAliasID, arg.Column2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ParentalConsent
	for rows.Next() {
		var i ParentalConsent
		if err := rows.Scan(
			&i.ConsentID,
			&i.StudentAliasID,
			&i.EventType,
			&i.Scope,
			&i.ValidFrom,
			&i.ValidUntil,
			&i.Version,
			&i.CreatedAt,
			&i.RecordedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockConsentChain = `-- name: LockConsentChain :exec

SELECT pg_advisory_xact_lock(hashtextextended('parental_consent:' || $1::text || ':' || $2::text, 0))
`

type LockConsentChainParams struct {
	Column1 string
	Column2 string
}

// T-W5-011（SQL-2）：家长授权账（parental_consent）的并发安全读写语句面。
// 版本分配临界区 = 调用方显式事务内的完整序列（D11 领域服务不自 commit）：
//
//	LockConsentChain（per-chain advisory xact lock，串行化同一
//	(student_alias_id, purpose) 授权链的全部写入者，含「首事件无行可锁」竞态）
//	→ GetLatestConsentEvent（读链顶版本与撤回前置校验）→ InsertConsentEvent
//	（版本 = 链顶+1 写入新事件）。
//
// 唯一索引 uq_parental_consent_version_per_purpose（0027）是最后防线：其拒绝以
// SQLSTATE 23505 到达应用层并被翻译为哨兵错误 ErrConsentConflict，异常不泄漏。
// append-only 纪律：本文件只有 INSERT/SELECT——UPDATE/DELETE 无查询面可写，
// 0015 的 trg_parental_consent_append_only 触发器物理兜底。
// per-chain 事务级 advisory 排他锁（pg_advisory_xact_lock 返回 void，
// 事务结束自动释放）。键含 student_alias_id 与 purpose 二级粒度：
// 不同学生、不同 purpose 的授权链互不阻塞。
func (q *Queries) LockConsentChain(ctx context.Context, arg LockConsentChainParams) error {
	_, err := q.db.Exec(ctx, lockConsentChain, arg.Column1, arg.Column2)
	return err
}
