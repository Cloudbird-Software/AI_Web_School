// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: session.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const advanceSessionAfterSubmit = `-- name: AdvanceSessionAfterSubmit :exec
UPDATE practice_session SET
	current_index = current_index + 1,
	answered_count = answered_count + 1,
	last_activity_at = $2,
	status = CASE
		WHEN current_index + 1 >= jsonb_array_length(item_sequence) AND NOT retest_wrong
			THEN 'completed'
		ELSE status
	END,
	completed_at = CASE
		WHEN current_index + 1 >= jsonb_array_length(item_sequence) AND NOT retest_wrong
			THEN $2
		ELSE completed_at
	END
WHERE session_id = $1
`

type AdvanceSessionAfterSubmitParams struct {
	SessionID      pgtype.UUID
	LastActivityAt pgtype.Timestamptz
}

// 提交推进：current_index/answered_count 各恰 +1 + 活动时刻（board 验收
// 「current_index 恰好推进 1」的物理面）。完结判定与 Python 冻结实现同构：
// 主序列走完且未开启回测 → completed（开回测的会话进入回测轮，归会话状态
// 机域，本卡不推进其完结）。CASE 引用的是本行旧值（UPDATE 语义）.
func (q *Queries) AdvanceSessionAfterSubmit(ctx context.Context, arg AdvanceSessionAfterSubmitParams) error {
	_, err := q.db.Exec(ctx, advanceSessionAfterSubmit, arg.SessionID, arg.LastActivityAt)
	return err
}

const getSessionForSubmit = `-- name: GetSessionForSubmit :one
SELECT session_id, student_alias_id, scene, gradeband, status, paper_id, item_sequence, current_index, retest_wrong, wrong_marks, time_limit_sec, answered_count, correct_count, started_at, last_resume_at, last_activity_at, completed_at, created_at FROM practice_session WHERE session_id = $1 FOR UPDATE
`

// 会话行 FOR UPDATE（验收 #1：提交路径对会话行加锁后再校验题序与推进）；
// 与 advisory 锁构成双锁分层（序恒定：先 advisory 后行锁，无死锁面）.
func (q *Queries) GetSessionForSubmit(ctx context.Context, sessionID pgtype.UUID) (PracticeSession, error) {
	row := q.db.QueryRow(ctx, getSessionForSubmit, sessionID)
	var i PracticeSession
	err := row.Scan(
		&i.SessionID,
		&i.StudentAliasID,
		&i.Scene,
		&i.Gradeband,
		&i.Status,
		&i.PaperID,
		&i.ItemSequence,
		&i.CurrentIndex,
		&i.RetestWrong,
		&i.WrongMarks,
		&i.TimeLimitSec,
		&i.AnsweredCount,
		&i.CorrectCount,
		&i.StartedAt,
		&i.LastResumeAt,
		&i.LastActivityAt,
		&i.CompletedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getSubmissionByIdempotencyKey = `-- name: GetSubmissionByIdempotencyKey :one
SELECT event_id, event_created_at FROM response_submission
WHERE session_id = $1 AND item_version_id = $2 AND answer_digest = $3
`

type GetSubmissionByIdempotencyKeyParams struct {
	SessionID     pgtype.UUID
	ItemVersionID string
	AnswerDigest  string
}

type GetSubmissionByIdempotencyKeyRow struct {
	EventID        pgtype.UUID
	EventCreatedAt pgtype.Timestamptz
}

// 幂等判定：三元组键恰一回指首次事件（复合主键保证确定性）。无行 =
// pgx.ErrNoRows（调用方按未命中继续提交流程）.
func (q *Queries) GetSubmissionByIdempotencyKey(ctx context.Context, arg GetSubmissionByIdempotencyKeyParams) (GetSubmissionByIdempotencyKeyRow, error) {
	row := q.db.QueryRow(ctx, getSubmissionByIdempotencyKey, arg.SessionID, arg.ItemVersionID, arg.AnswerDigest)
	var i GetSubmissionByIdempotencyKeyRow
	err := row.Scan(&i.EventID, &i.EventCreatedAt)
	return i, err
}

const insertResponseSubmission = `-- name: InsertResponseSubmission :exec
INSERT INTO response_submission (
	session_id, item_version_id, answer_digest, event_id, event_created_at, created_at
) VALUES ($1, $2, $3, $4, $5, $6)
`

type InsertResponseSubmissionParams struct {
	SessionID      pgtype.UUID
	ItemVersionID  string
	AnswerDigest   string
	EventID        pgtype.UUID
	EventCreatedAt pgtype.Timestamptz
	CreatedAt      pgtype.Timestamptz
}

// 幂等登记（与事件同一事务：登记账行永远回指已入账的真实事件——FK 兜底）；
// created_at 显式传值使登记时刻与事件时刻、内存实现逐字段同构.
func (q *Queries) InsertResponseSubmission(ctx context.Context, arg InsertResponseSubmissionParams) error {
	_, err := q.db.Exec(ctx, insertResponseSubmission,
		arg.SessionID,
		arg.ItemVersionID,
		arg.AnswerDigest,
		arg.EventID,
		arg.EventCreatedAt,
		arg.CreatedAt,
	)
	return err
}

const lockSessionSubmission = `-- name: LockSessionSubmission :exec

SELECT pg_advisory_xact_lock(hashtextextended('practice_session:' || $1::text, 0))
`

// T-W5-018（SQL-2）：会话作答提交（幂等 + 并发安全）的语句面。
// 提交临界区 = 调用方显式事务内的完整序列（D11 领域服务不自 commit）：
//
//	LockSessionSubmission（per-session advisory xact lock，串行化同一会话的
//	全部提交者，含「首次提交无登记行可锁」竞态）→ GetSubmissionByIdempotencyKey
//	（幂等判定先行：命中即原样返回首次事件、零副作用）→ GetSessionForSubmit
//	（会话行 FOR UPDATE，验收 #1 的行锁字面语义）→ 状态/时长/题序校验 →
//	事件入账（core/events.Writer 经 events.sql 的 InsertResponseEvent）→
//	InsertResponseSubmission（幂等登记）→ AdvanceSessionAfterSubmit（推进）。
//
// 幂等唯一性防线：pk_response_submission (session_id, item_version_id,
// answer_digest)（0032）——绕过应用锁的重复写入以 SQLSTATE 23505 明确拒绝，
// core/session 映射为哨兵 ErrSubmissionConflict，异常不泄漏。
// practice_session 是运行态账（0011）：current_index/answered_count/status 等
// 运行态字段可更新（题序等身份字段不可变由迁移触发器物理兜底）；本文件的
// UPDATE 只触运行态列——两表 INSERT/SELECT 面不含任何账本改写语句.
// per-session 事务级 advisory 排他锁（pg_advisory_xact_lock 返回 void，事务
// 结束自动释放）。键 = practice_session 域前缀 + 会话 id：不同会话互不阻塞.
func (q *Queries) LockSessionSubmission(ctx context.Context, dollar_1 string) error {
	_, err := q.db.Exec(ctx, lockSessionSubmission, dollar_1)
	return err
}

const markSessionRestPrompted = `-- name: MarkSessionRestPrompted :exec
UPDATE practice_session SET status = 'rest_prompted' WHERE session_id = $1
`

// 时长保护置位（Python _check_time_protection 同语义）：拒绝提交但留下
// rest_prompted 状态（resume 重置计时后恢复作答）。只动状态列，零事件写入.
func (q *Queries) MarkSessionRestPrompted(ctx context.Context, sessionID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, markSessionRestPrompted, sessionID)
	return err
}
