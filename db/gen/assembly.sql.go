// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: assembly.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const insertPaperExposure = `-- name: InsertPaperExposure :exec
INSERT INTO paper_exposure (
	exposure_id, channel, subject_pack_id, textbook_version, gradeband,
	week_label, item_version_id, template_version_id, paper_id
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
`

type InsertPaperExposureParams struct {
	ExposureID        string
	Channel           string
	SubjectPackID     string
	TextbookVersion   pgtype.Text
	Gradeband         string
	WeekLabel         string
	ItemVersionID     string
	TemplateVersionID pgtype.Text
	PaperID           pgtype.Text
}

// 静态轨曝光预留入账（与 paper/paper_item 写入同一事务，序自由——FK 一致性
// 由 0010 外键在语句级验证）。exposure_id 由应用侧发号（crypto/rand，与
// estimator 的 run_id 同惯例）；created_at 取 DB 默认 now()。并发组卷的重复
// 曝光由 uq_paper_exposure_queue_item UNIQUE 兜底（23505），应用层查询只是
// 热路径优化。
func (q *Queries) InsertPaperExposure(ctx context.Context, arg InsertPaperExposureParams) error {
	_, err := q.db.Exec(ctx, insertPaperExposure,
		arg.ExposureID,
		arg.Channel,
		arg.SubjectPackID,
		arg.TextbookVersion,
		arg.Gradeband,
		arg.WeekLabel,
		arg.ItemVersionID,
		arg.TemplateVersionID,
		arg.PaperID,
	)
	return err
}

const insertStudentExposure = `-- name: InsertStudentExposure :exec
INSERT INTO student_exposure (
	exposure_id, student_alias_id, item_version_id, template_version_id,
	paper_id, session_id, purpose
) VALUES ($1, $2, $3, $4, $5, $6, $7)
`

type InsertStudentExposureParams struct {
	ExposureID        string
	StudentAliasID    string
	ItemVersionID     string
	TemplateVersionID pgtype.Text
	PaperID           pgtype.Text
	SessionID         pgtype.Text
	Purpose           string
}

// 在线轨曝光预留入账：把发给学生（匿名 id）的题登记到学生轨。uq_student_
// exposure_student_item UNIQUE 兜底；purpose 值域由 ck_student_exposure_
// purpose_domain CHECK 兜底。
func (q *Queries) InsertStudentExposure(ctx context.Context, arg InsertStudentExposureParams) error {
	_, err := q.db.Exec(ctx, insertStudentExposure,
		arg.ExposureID,
		arg.StudentAliasID,
		arg.ItemVersionID,
		arg.TemplateVersionID,
		arg.PaperID,
		arg.SessionID,
		arg.Purpose,
	)
	return err
}

const loadServingCandidates = `-- name: LoadServingCandidates :many

SELECT pack_id, item_version_id, item_id, template_version_id,
       objective, interaction_ref, lineage
FROM v_serving_item_version
WHERE pack_id = $1 AND objective->>'gradeband' = $2::text
`

type LoadServingCandidatesParams struct {
	PackID    string
	Gradeband string
}

type LoadServingCandidatesRow struct {
	PackID            string
	ItemVersionID     string
	ItemID            string
	TemplateVersionID pgtype.Text
	Objective         []byte
	InteractionRef    []byte
	Lineage           []byte
}

// 组卷域查询面（审计卡 #151）：候选池 serving 视图读取 + 曝光账本双轨读写。
// 表结构与约束由迁移承载：serving 视图（0008，published 过滤的单一事实源）、
// paper_exposure / student_exposure（0010，append-only 触发器物理兜底）。
// 事务纪律（S4/D11）：查询语句面向连接池（组卷读取阶段取数）；两条 INSERT
// 必须运行在调用方已 begin 的显式事务内（core/assembly.PGExposureStore 经
// WithTx 绑定传入，与 paper/paper_item 写入同进同退），提交/回滚由最外层
// 调用方统一持有。两账只增不改（D1）：本文件只有 INSERT 与 SELECT——
// UPDATE/DELETE 无查询面可写，DB 触发器兜底。
// 候选池 serving 视图查询（CandidateStore.LoadCandidates 的取数语句）：
// published 过滤由视图 WHERE 承担（0008：status='published' AND published_at
// IS NOT NULL AND retired_at IS NULL——未过校验门的产物进不了 serving），
// 本语句只做蓝图维度过滤：学科包 × 学段（objective->>'gradeband'，与冻结
// _SERVING_POOL_SQL 的 WHERE 同判据）。列投影取 CandidateFromServingRow 的
// 最小消费集；无 ORDER BY——结果序交由求解器稳定哈希排序，确定性不依赖
// DB 返回序（Memory 实现的插入序语义同源）。
func (q *Queries) LoadServingCandidates(ctx context.Context, arg LoadServingCandidatesParams) ([]LoadServingCandidatesRow, error) {
	rows, err := q.db.Query(ctx, loadServingCandidates, arg.PackID, arg.Gradeband)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LoadServingCandidatesRow
	for rows.Next() {
		var i LoadServingCandidatesRow
		if err := rows.Scan(
			&i.PackID,
			&i.ItemVersionID,
			&i.ItemID,
			&i.TemplateVersionID,
			&i.Objective,
			&i.InteractionRef,
			&i.Lineage,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const queueExposedItemVersionIDs = `-- name: QueueExposedItemVersionIDs :many
SELECT item_version_id FROM paper_exposure
WHERE channel = $1 AND subject_pack_id = $2 AND week_label = $3
`

type QueueExposedItemVersionIDsParams struct {
	Channel       string
	SubjectPackID string
	WeekLabel     string
}

// 静态轨：某 渠道×学科包×周队列 已曝光的题目版本集（跨期不重复；
// ix_paper_exposure_queue 前缀命中）。
func (q *Queries) QueueExposedItemVersionIDs(ctx context.Context, arg QueueExposedItemVersionIDsParams) ([]string, error) {
	rows, err := q.db.Query(ctx, queueExposedItemVersionIDs, arg.Channel, arg.SubjectPackID, arg.WeekLabel)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var item_version_id string
		if err := rows.Scan(&item_version_id); err != nil {
			return nil, err
		}
		items = append(items, item_version_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const queueExposedTemplateVersionIDs = `-- name: QueueExposedTemplateVersionIDs :many
SELECT template_version_id FROM paper_exposure
WHERE channel = $1 AND subject_pack_id = $2 AND week_label = $3
  AND template_version_id IS NOT NULL
`

type QueueExposedTemplateVersionIDsParams struct {
	Channel       string
	SubjectPackID string
	WeekLabel     string
}

// 静态轨：同队列已曝光的母题版本集（同母题不同卷；IS NOT NULL 与冻结 SQL
// 同义——无母题的裸题不计入母题互斥集）。
func (q *Queries) QueueExposedTemplateVersionIDs(ctx context.Context, arg QueueExposedTemplateVersionIDsParams) ([]pgtype.Text, error) {
	rows, err := q.db.Query(ctx, queueExposedTemplateVersionIDs, arg.Channel, arg.SubjectPackID, arg.WeekLabel)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []pgtype.Text
	for rows.Next() {
		var template_version_id pgtype.Text
		if err := rows.Scan(&template_version_id); err != nil {
			return nil, err
		}
		items = append(items, template_version_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const studentExposedItemVersionIDs = `-- name: StudentExposedItemVersionIDs :many
SELECT item_version_id FROM student_exposure
WHERE student_alias_id = $1
`

// 在线轨：某学生（匿名 id，D7）已见过的题目版本集（跨期不重复；
// ix_student_exposure_student 命中）。
func (q *Queries) StudentExposedItemVersionIDs(ctx context.Context, studentAliasID string) ([]string, error) {
	rows, err := q.db.Query(ctx, studentExposedItemVersionIDs, studentAliasID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var item_version_id string
		if err := rows.Scan(&item_version_id); err != nil {
			return nil, err
		}
		items = append(items, item_version_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const studentExposedTemplateVersionIDs = `-- name: StudentExposedTemplateVersionIDs :many
SELECT template_version_id FROM student_exposure
WHERE student_alias_id = $1 AND template_version_id IS NOT NULL
`

// 在线轨：某学生已见过的母题版本集（同母题不同卷；NULL 模板不计）。
func (q *Queries) StudentExposedTemplateVersionIDs(ctx context.Context, studentAliasID string) ([]pgtype.Text, error) {
	rows, err := q.db.Query(ctx, studentExposedTemplateVersionIDs, studentAliasID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []pgtype.Text
	for rows.Next() {
		var template_version_id pgtype.Text
		if err := rows.Scan(&template_version_id); err != nil {
			return nil, err
		}
		items = append(items, template_version_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
