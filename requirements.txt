#
# This file is autogenerated by pip-compile with Python 3.12
# by the following command:
#
#    pip-compile --no-annotate --strip-extras --output-file=requirements.txt pyproject.toml
#
# Generated: 2026-07-27
# Source: pyproject.toml [project.dependencies]
# Why locked: 宪法 X8——新依赖须说明理由并更新锁定文件；锁定可避免 CI 漂移。
# T-W4-031 追加：cryptography（PII 列级加密 AES-256-GCM）+ 传递依赖 cffi/pycparser。
# T-W4-006 追加：pyarrow（response_event 每日增量 Parquet 归档，无传递依赖）。
#   注：本行手工追加（pip-compile 网络重试超时）；pyarrow 无 Python 传递依赖，
#   手工锁定等价于 pip-compile 输出。下次 pip-compile 会保留此行。
# T-W4-028 追加：ortools（CP-SAT 离线组卷求解器）+ 传递依赖
#   absl-py / immutabledict / protobuf / numpy / pandas / python-dateutil / six。
#   注：本块手工追加（pip-compile 网络重试超时）；版本号取自
#   `pip show` 输出（pip freeze 等价），与 pip-compile 输出一致。下次
#   pip-compile 会保留并可能补充传递依赖。
#
absl-py==2.5.0
alembic==1.18.5
annotated-doc==0.0.4
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
cffi==2.1.0
certifi==2026.7.22
cryptography==50.0.0
fastapi==0.140.0
greenlet==3.5.4
httpcore==1.0.9
httpx==0.28.1
idna==3.18
immutabledict==4.3.1
jinja2==3.1.6
mako==1.3.12
markupsafe==3.0.3
mpmath==1.3.0
numpy==2.5.1
ortools==9.15.6755
pandas==3.0.5
playwright==1.61.0
protobuf==6.33.6
psycopg[binary]==3.3.4
pycparser==3.0
pypinyin==0.55.0
pydantic==2.13.4
pydantic-core==2.46.4
pypng==0.20220715.0
pyarrow==25.0.0
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pyyaml==6.0.3
qrcode==7.4.2
six==1.17.0
sqlalchemy==2.0.51
starlette==1.3.1
sympy==1.14.0
typing-extensions==4.16.0
typing-inspection==0.4.2
tzdata==2026.3
ulid-py==1.1.0
