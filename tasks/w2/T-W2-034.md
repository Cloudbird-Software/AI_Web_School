# 任务卡
```yaml
id: T-W2-034
wave: W2
title: HTML/CSS 渲染器 + KaTeX/SVG 集成
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6]
context_paths: [src/core/render/html_renderer.py, src/core/render/templates/item.html]
deliverables: [src/core/render/html_renderer.py, src/core/render/templates/item.html, tests/unit/test_html_renderer.py]
acceptance: make accept TASK=T-W2-034
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/render
depends_on: [T-W2-033]
non_goals: [PDF 分页引擎, 在线交互, 小程序组件]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 Render IR → HTML/CSS 渲染器，集成 KaTeX（数学公式）与 SVG（学科组件）。
生成可嵌入试卷页面的 item HTML 片段。

## 验收标准（逐条可执行）
1. `html_renderer.py` 将 RenderIR 转为 HTML 字符串，含 item 容器、题号、选项列表、填空下划线。
2. 数学公式块渲染为 KaTeX 兼容标记；SVG 块原样嵌入。
3. 单元测试覆盖文本、选择、填空、公式、SVG 五种 block。
4. HTML 输出可通过 DOMPurify 风格白名单校验（无 script/onclick）。
