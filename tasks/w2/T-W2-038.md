# 任务卡
```yaml
id: T-W2-038
wave: W2
title: 周更静态批处理管线 v1
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6]
context_paths: [src/core/render/weekly_batch.py, tests/unit/test_weekly_batch.py]
deliverables: [src/core/render/weekly_batch.py, tests/unit/test_weekly_batch.py]
acceptance: make accept TASK=T-W2-038
accept_script:
model_floor: T1
token_budget: 500k
owner_module: src/core/render
depends_on: [T-W2-004, T-W2-011, T-W2-035, T-W2-037]
non_goals: [教学上下文服务, CP-SAT 求解器, 按人自适应]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现周更批处理 v1：手工指定范围快照 → 按约束清单选题 → 组卷 → 渲染 → 发布。
生成真实 PDF 试卷与解析册，固化 paper/paper_item 行。

## 验收标准（逐条可执行）
1. `weekly_batch.run(scope, constraints, output_dir)` 返回 paper_id 与 PDF 路径。
2. 选题约束包括：学科/年级/题量/交互类型分布；W2 用确定性装填（非求解器）。
3. 生成试卷 PDF 与解析册 PDF，卷码 QR 与题短码可扫描回溯。
4. 单元测试用 mock 渲染器验证 paper_item 映射与追溯链。
