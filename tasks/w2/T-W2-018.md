# 任务卡
```yaml
id: T-W2-018
wave: W2
title: publish_corpus_asset 门字段对齐修复
spec: [specs/contracts/db/item-model.md#2.5, specs/contracts/db/item-model.md#4]
context_paths: [src/core/content/writer.py, tests/unit/test_content_writer.py]
deliverables: [src/core/content/writer.py（修复）, tests/unit/test_content_writer.py（补充断言）]
acceptance: make accept TASK=T-W2-018
accept_script:
model_floor: T1
token_budget: 200k
owner_module: src/core/content
depends_on: [T-W1-007, T-W2-015]
non_goals: [重写内容写入服务, 新增内容类型]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
修复 W1 遗留：publish_corpus_asset 写入 corpus_version 时对齐 material 两段式门字段。
补充 status/gate_certificate_id/published_at 的强制校验与测试断言。

## 验收标准（逐条可执行）
1. `publish_corpus_asset` 创建 corpus_asset + corpus_version，status 机制与 material 一致。
2. published 状态必须提供 gate_certificate_id，否则抛出 GateEnforcementError。
3. 现有 content_writer 单测不退化，新增 corpus 门字段断言通过。
