# 任务卡
```yaml
id: T-W2-010
wave: W2
title: 门编排引擎（队列异步/廉价先行/阻断短路）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.3]
context_paths: [src/core/gate/orchestrator/, src/core/gate/policy/loader.py]
deliverables: [src/core/gate/orchestrator/orchestrator.py, tests/unit/test_gate_orchestrator.py]
acceptance: make accept TASK=T-W2-010
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/gate/orchestrator
depends_on: [T-W2-007, T-W2-008, T-W2-009]   # 009 通用验证器链是编排的执行对象（终审补）
non_goals: [真实 Redis 队列生产部署, 分布式 worker, 多租户隔离]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现门编排：按策略链顺序调用验证器，廉价先行，任一阻断项 fail 即短路。
记录 gate_run + gate_verdict（写入 gate 表），成本与证据留痕。
支持同步与异步（Redis 任务队列占位）两种模式。

## 验收标准（逐条可执行）
1. `run_gate(artifact_ref, artifact_type, pack_id, ctx)` 返回最终 verdict 与 cert_id（若通过）。
2. 失败短路：链中第一个 fail 之后的验证器不被调用（单元测试 mock 验证）。
3. 通过时写入 GateCertificate + GateRun + GateVerdict；失败时只写入 GateRun + GateVerdict。
4. 单元测试覆盖 pass/fail/short-circuit/review 四种情况。
