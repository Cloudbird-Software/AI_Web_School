# 任务卡
```yaml
id: T-W2-025
wave: W2
title: 数学变量类型与安全函数库
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#5.1, ADR/母题与知识体系-工程架构设计方案v2.md#5.2, specs/contracts/registries/scorer.yaml]
context_paths: [src/packs/subject-math/variable-types/]
deliverables: [src/packs/subject-math/variable-types/variable_types.py, src/packs/subject-math/variable-types/functions.py, tests/unit/test_math_functions.py]
acceptance: make accept TASK=T-W2-025
accept_script:                  # 置空：tests/acceptance/ 不存在；验收由 tests/unit/test_math_functions.py + 通用门禁承载
model_floor: T1
token_budget: 400k
owner_module: src/packs/subject-math/variable-types   # 终审修：与 026-030 子目录 owner 消除包根前缀重叠
depends_on: [T-W2-002]
non_goals: [几何证明, 统计推断, 符号代数 CAS]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
按 SubjectPack 契约落地数学变量类型（整数、分数、小数、带单位量、区间）。
实现安全函数库（版本化、全量单测），作为 DSL answer_program 的扩展函数源。

## 验收标准（逐条可执行）
1. `variable_types.py` 定义 `MathInteger/Fraction/Decimal/Quantity/Interval`，支持规范化与相等比较。
2. `functions.py` 实现 ≥15 个函数（四则、化简、单位换算、最大公约、取整等），每个含 docstring 与类型标注。
3. 全量单测覆盖每个函数的边界与错误输入。
4. 不 import 核心域未暴露的模块。
