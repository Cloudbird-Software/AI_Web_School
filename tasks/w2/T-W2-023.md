# 任务卡
```yaml
id: T-W2-023
wave: W2
title: 数学母题 30 题实例化期望输出
spec: [ADR/OPC-AI原生开发组织方案.md#5（防线5 黄金数据集回归）]
context_paths: [tests/golden/items/math/, tests/golden/schema.yaml]
deliverables: [tests/golden/items/math/*.yaml（30 题，每题内含 template/params/expected，自洽于 T-W2-022 schema）]
acceptance: make accept TASK=T-W2-023
accept_script:                  # 置空：tests/acceptance/ 不存在；验收由 T-W2-022 schema 加载器校验 + 通用门禁承载
model_floor: T2
token_budget: 300k
owner_module: tests/golden
depends_on: [T-W2-022, T-W2-025]
non_goals: [非数学题型, 评分答案, PDF 渲染]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
编写 30 个数学 3-4 年级真实母题，覆盖数值填空、单选、多选、判断、分步过程、匹配连线等交互。
每题含期望实例化输出，用于 golden 回归。

## 验收标准（逐条可执行）
1. `tests/golden/items/math/` 下 ≥30 个 YAML，每个均通过 schema 校验。
2. 每个 YAML 的 expected_item_version_id 由实际引擎实例化确认后固定。
3. 覆盖 ≥6 种不同交互类型。
