# 任务卡
```yaml
id: T-W2-033
wave: W2
title: Render IR v1 schema 与 Item AST 映射
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6]
context_paths: [src/core/render/ir.py, src/core/render/item_to_ir.py]
deliverables: [src/core/render/ir.py, src/core/render/item_to_ir.py, tests/unit/test_render_ir.py]
acceptance: make accept TASK=T-W2-033
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/render
depends_on: []
non_goals: [HTML/CSS 输出, PDF 导出, 在线组件]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
定义 Render IR v1：内容与样式分离的中间态（Item AST + 版式提示）。
实现 ItemVersion → Render IR 的映射，支持文本块、填空、选择、数学 SVG、题组结构。

## 验收标准（逐条可执行）
1. `ir.py` 定义 `RenderIR` Pydantic schema：blocks（text/fill/choice/math_svg/group）+ layout_hints。
2. `item_to_ir.py` 将 ItemVersion.content 转换为 RenderIR，保留题号、选项、填空位置。
3. 单元测试覆盖单选、文本填空、数学 SVG、题组四种结构。
4. 不 import 任何学科包/学段包。
