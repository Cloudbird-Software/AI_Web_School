# 任务卡
```yaml
id: T-W2-001
wave: W2
title: 母题 DSL v1 模式与 Linter（六大块）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.1, specs/contracts/registries/interaction.yaml]
context_paths: [src/core/instantiation/dsl/, tests/unit/test_dsl_linter.py]
deliverables: [src/core/instantiation/dsl/schema.py, src/core/instantiation/dsl/linter.py, tests/unit/test_dsl_linter.py]
acceptance: make accept TASK=T-W2-001
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/instantiation/dsl
depends_on: []
non_goals: [实例化执行, 安全表达式求值, 渲染组件]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
落地母题 DSL v1 的 JSON Schema 与 Linter，覆盖 objective/slots/variation_axes/presentation/answer_program/distractor_rules 六大块。
对 slot 类型、difficulty_relevant 标志、变式轴声明做静态校验，输出结构化错误。
为后续实例化引擎提供可复用的 DSL 校验入口。

## 验收标准（逐条可执行）
1. `src/core/instantiation/dsl/schema.py` 包含 DSL 六大块的 Pydantic/JSON Schema 定义，extra='forbid'。
2. `src/core/instantiation/dsl/linter.py` 提供 `lint(spec)`，返回 `{valid, errors[]}`，至少覆盖：必填块缺失、slot 类型不在允许列表、variation_axis 引用不存在的 slot、difficulty_relevant 不是 boolean。
3. 单元测试覆盖 3 个有效母题 + 5 种典型错误，全部通过。
4. `make accept TASK=T-W2-001` 全绿。
5. 不 import 任何学科包/学段包。
