# 任务卡
```yaml
id: T-W2-004
wave: W2
title: 确定性实例化引擎 + 内容寻址（公式一）
spec: [specs/contracts/db/item-model.md#3, ADR/母题与知识体系-工程架构设计方案v2.md#2.2, ADR/母题与知识体系-工程架构设计方案v2.md#4.1]
context_paths: [src/core/instantiation/engine/, src/core/models/content_addressing.py, tests/unit/test_instantiation_engine.py]
deliverables: [src/core/instantiation/engine/engine.py, tests/unit/test_instantiation_engine.py, tests/golden/instantiation/sample_*.yaml]
acceptance: make accept TASK=T-W2-004
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/instantiation/engine
depends_on: [T-W2-001, T-W2-002, T-W2-003]
non_goals: [受控变式, 门签发, 渲染快照, B 线装配]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现母题到 ItemVersion 的确定性实例化：按 params 填充 slots、求值 answer_program、生成干扰项、装配 content。
A/B 级实例 id 严格按内容寻址公式一计算（H(template_version_digest, normalized_params, pack_digest, engine_digest, corpus_digests, locale)）。
规范化 params 使用定点/分数，禁浮点漂移；同一输入必须得到同一 id。

## 验收标准（逐条可执行）
1. `instantiate(template_version, params, seed=0)` 返回完整 ItemVersion dict（含 objective/interaction_ref/content/scoring_ref/error_bindings/lineage）。
2. A/B 级 `item_version_id` 与公式一一致；两次相同输入返回同一 id（golden 样例回归）。
3. 至少提供 3 个黄金样例（单选/数值填空/匹配连线各 1），期望输出与实例化结果逐字节一致。
4. `make accept TASK=T-W2-004` 全绿。
5. 不 import 任何学科包/学段包。
