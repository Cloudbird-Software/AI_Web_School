# 任务卡
```yaml
id: T-W2-009
wave: W2
title: 通用验证器 v1（结构/许可/查重占位）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.3, specs/contracts/db/item-model.md#4]
context_paths: [src/core/gate/validators/, tests/unit/test_generic_validators.py]
deliverables: [src/core/gate/validators/generic.py, tests/unit/test_generic_validators.py]
acceptance: make accept TASK=T-W2-009
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/gate/validators
depends_on: [T-W2-007]
non_goals: [语义查重, 外部真题指纹, 适龄安全强模型裁判]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现三个通用验证器：SchemaValidator（结构/schema）、LicenseValidator（来源许可存在且未过期）、DuplicatePlaceholderValidator（规范化哈希占位查重）。
三个验证器均按统一契约返回结果与证据。
为数学双实现验算等学科验证器留出编排位置。

## 验收标准（逐条可执行）
1. `SchemaValidator` 调用 DSL Linter / JSON Schema 校验，返回 pass/fail。
2. `LicenseValidator` 检查 material_version/corpus_version 的 license_id 存在且 decision=approved、未过期。
3. `DuplicatePlaceholderValidator` 计算 canonical hash，与已 published 版本比对，重复则 review。
4. 单元测试覆盖通过、结构失败、许可失败、重复提示四种情况。
