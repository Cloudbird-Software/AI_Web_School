# 任务卡
```yaml
id: T-W2-037
wave: W2
title: paper / paper_item 表 + 卷码/QR/题短码
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6, ADR/母题与知识体系-工程架构设计方案v2.md#附录A]
context_paths: [alembic/versions/0007_paper_trace.py, src/core/models/paper.py, src/core/models/paper_item.py]
deliverables: [alembic/versions/0007_paper_trace.py, src/core/models/paper.py, src/core/models/paper_item.py, src/core/render/trace_codes.py, tests/unit/test_paper_trace.py]
acceptance: make accept TASK=T-W2-037
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/render
depends_on: [T-W1-007]
non_goals: [在线试卷表, 学生作答表, 解析册独立表]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
创建 paper / paper_item 迁移，固化「卷→题→版本」映射。
实现卷码（paper code）生成、QR 码（只含卷 Spec ID+校验位）、题短码生成与校验。

## 验收标准（逐条可执行）
1. 迁移 0007 创建 `paper`/`paper_item` 表，`paper_item` 含 item_version_id 与 placement_token。
2. `trace_codes.py` 提供 `generate_paper_code()`、`generate_qr_payload(paper_spec_id)`、`generate_item_short_code(...)`，QR payload 不含实例明文。
3. 短码可校验并回溯到 paper_item → item_version → gate_certificate → 签发人。
4. 单元测试覆盖生成、校验、回溯。
