# 任务卡
```yaml
id: T-W2-008
wave: W2
title: 门策略矩阵 schema 与加载器（学科×产物类型）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.3]
context_paths: [src/core/gate/policy/, specs/contracts/gate/policy-schema.yaml]
deliverables: [src/core/gate/policy/loader.py, specs/contracts/gate/policy-schema.yaml, tests/unit/test_gate_policy.py]
acceptance: make accept TASK=T-W2-008
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/gate/policy
depends_on: [T-W2-007]
non_goals: [策略版本灰度, AQL 抽检, 影子运行]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
落地门策略矩阵 schema：按 pack_id × artifact_type 配置有序验证器链，策略版本化。
实现加载器与解析校验，W2 先提供 `generic` 与 `subject-math` 两条链 skeleton。
策略文件为 YAML，人类可读，可版本化。

## 验收标准（逐条可执行）
1. `specs/contracts/gate/policy-schema.yaml` 定义策略矩阵 schema（artifact_type ∈ item/material/corpus/group/blueprint/audio）。
2. `GatePolicy.load(path)` 返回策略对象，校验必填字段与 validator_id 存在性（在注册表中声明即可，不强求实现）。
3. W2 默认策略至少包含 generic 链：schema/linter → license → duplicate_placeholder。
4. 单元测试覆盖策略加载、版本校验、缺失 validator 报错。
