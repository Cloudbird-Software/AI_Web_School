# 任务卡
```yaml
id: T-W2-045
wave: W2
title: Makefile demo-w2 与 nightly W2 检查项
spec: [ADR/OPC-AI原生开发组织方案.md#2.2]
context_paths: [Makefile, .github/workflows/nightly.yml]
deliverables: [Makefile（加 demo-w2）, .github/workflows/nightly.yml（加 W2 检查）]
acceptance: make accept TASK=T-W2-045
accept_script: scripts/wave-exit/w2.sh
model_floor: T2
token_budget: 100k
owner_module: repo-root
depends_on: [T-W2-044]
non_goals: [部署 pipeline, release 自动化, 多环境 matrix]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
Makefile 增加 `demo-w2` 目标，等价于 `bash scripts/wave-exit/w2.sh`。
nightly workflow 增加 W2 检查项，确保黄金路径常绿。

## 验收标准（逐条可执行）
1. `make demo-w2` 成功执行 `scripts/wave-exit/w2.sh`。
2. `.github/workflows/nightly.yml` 包含 W2 出口步骤。
3. `make accept TASK=T-W2-045` 验证 Makefile 目标与 workflow 语法。
