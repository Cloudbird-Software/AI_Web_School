# 任务卡
```yaml
id: T-W2-017
wave: W2
title: B 线语料装配线框架 + 单位换算题型实证
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.1]
context_paths: [src/core/production/b_assembler.py, tests/unit/test_b_assembler.py]
deliverables: [src/core/production/b_assembler.py, tests/unit/test_b_assembler.py, tests/golden/b_line/unit_conversion.yaml]
acceptance: make accept TASK=T-W2-017
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/production
depends_on: [T-W2-004, T-W2-016]
non_goals: [C/D 线工坊, 完整 B 线 DSL, 语文/英语 B 线]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 B 线装配线 v1：框架模板 + 语料库填充，产出统一 ItemVersion。
以单位换算题型为实证，走通「框架模板→语料填充→实例化→门→入库」。
B 线产物同样携带完整谱系（corpus_refs）。

## 验收标准（逐条可执行）
1. `b_assembler.assemble(template, corpus_refs, params)` 返回 ItemVersion dict。
2. 单位换算实证模板与 3 组语料可生成 3 个不同 ItemVersion，且 id 按公式二/一稳定。
3. 产物 lineage.corpus_refs 非空，且 digest 与 corpus_version_id 对应。
4. 单元测试覆盖装配、缺语料、内容寻址稳定性。
