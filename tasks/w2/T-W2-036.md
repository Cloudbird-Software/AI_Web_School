# 任务卡
```yaml
id: T-W2-036
wave: W2
title: 品牌模板 v1（页面模板 + CSS）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6]
context_paths: [src/core/render/brand/default.css, src/core/render/brand/page.html]
deliverables: [src/core/render/brand/default.css, src/core/render/brand/page.html]
acceptance: make accept TASK=T-W2-036
accept_script:
model_floor: T2
token_budget: 150k
owner_module: src/core/render
depends_on: [T-W2-033]
non_goals: [多品牌动态切换, 设计师 UI 编辑器, 小程序样式]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
定义品牌模板 v1：A4 页面模板 + CSS，用于静态卷 PDF。
模板预留卷码、题短码、页眉页脚插槽。

## 验收标准（逐条可执行）
1. `default.css` 定义 A4 尺寸、字体、题号样式、选项排列、填空下划线。
2. `page.html` 是 Jinja2 模板，含 `{{ paper_code }}`、`{{ qr_svg }}`、`{{ items_html }}` 等插槽。
3. 模板可通过 HTML 渲染器填充并生成有效 HTML。
