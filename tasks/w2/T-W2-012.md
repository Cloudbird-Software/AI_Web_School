# 任务卡
```yaml
id: T-W2-012
wave: W2
title: KP node/edge/relation_type 表与 ORM 迁移
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.2, ADR/母题与知识体系-工程架构设计方案v2.md#附录A]
context_paths: [alembic/versions/0006_knowledge_graph.py, src/core/models/kp_node.py, src/core/models/kp_edge.py]
deliverables: [alembic/versions/0006_knowledge_graph.py, src/core/models/kp_node.py, src/core/models/kp_edge.py, src/core/models/relation_type.py]
acceptance: make accept TASK=T-W2-012
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/knowledge
depends_on: [T-W1-002]
non_goals: [传递闭包, graph_release, 种子数据]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
按知识域契约创建 kp_node / kp_edge / relation_type 表与 ORM。
支持 dimension 一等公民、类型化边（先修/易混淆/组成）、relation_type 元数据（directed/transitive/acyclic/symmetric）。
确保 code 字段可冻结语义，节点带 status/valid_from/valid_to/supersedes_id。

## 验收标准（逐条可执行）
1. 迁移 0006 创建 `kp_node`/`kp_edge`/`relation_type` 三表，字段与契约一致。
2. `kp_node` 含 pack_id/dimension/code/title/std_anchor/gradeband/status/valid_from/valid_to/supersedes_id。
3. `relation_type` 含 directed/transitive/acyclic/symmetric 布尔元数据。
4. `make migrate-check` 全绿。
