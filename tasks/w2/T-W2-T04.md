# 验证卡
```yaml
id: T-W2-T04
wave: W2
owner_module: tests/acceptance/w2b-chinese
title: 语文最小链路验证（看拼音写词语全链路）
validates: [T-W2-031, T-W2-032]
prerequisites: [T-W2-032]   # 传递覆盖 031 及其上游（来源登记/实例化/门基类）
parallel_group: W2b-学科包
model_floor: T1
token_budget: 400k
validation_script: tests/acceptance/w2b-chinese/test_w2_t04_chinese_pinyin.py   # 脚本随本卡验证执行时交付于同路径
```

## 验证策略
验证语文包最小实证：字词/拼音语料、看拼音写词语模板、规范字表验证器、实例化与渲染。E2E-4 的独立验收。
"模板→实例化→语文验证器→入库→渲染"全链路断言由本卡交付的验收脚本承载。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W2-T04: 语文最小链路验证（看拼音写词语全链路） =="
docker compose up -d --wait db
python -m pytest tests/unit/test_chinese_corpus.py -q
python -m pytest tests/unit/test_pinyin_to_words.py -q
# E2E-4 全链路（脚本随本卡验证执行时交付于同路径）
python -m pytest tests/acceptance/w2b-chinese/test_w2_t04_chinese_pinyin.py -q
echo "✅ T-W2-T04 验证通过"
```
