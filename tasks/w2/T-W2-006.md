# 任务卡
```yaml
id: T-W2-006
wave: W2
title: difficulty_relevant 槽变更检测与难度重估事件发布
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.1]
context_paths: [src/core/instantiation/difficulty/, src/core/events/writer.py]
deliverables: [src/core/instantiation/difficulty/trigger.py, tests/unit/test_difficulty_trigger.py]
acceptance: make accept TASK=T-W2-006
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T2
token_budget: 150k
owner_module: src/core/instantiation/difficulty
depends_on: [T-W2-004]
non_goals: [实际难度估计, 参数标定批处理, IRT 模型]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
在实例化后检测 params 是否变更了 difficulty_relevant 槽，若是则发布 difficulty_reestimate 事件。
事件进入任务队列（Redis），供 W3+ 参数标定消费；W2 只需落事件并验证 schema。
保持核心域学科无关。

## 验收标准（逐条可执行）
1. `detect_difficulty_change(template_version, params)` 返回布尔值，命中 difficulty_relevant 槽为 True。
2. 命中时调用事件写入服务写入 `difficulty_reestimate` 事件（JSON schema 在 specs/contracts/events/ 补登记）。
3. 单元测试覆盖变与不变两种场景。
4. 不 import 任何学科包/学段包。
