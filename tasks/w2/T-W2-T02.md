# 验证卡
```yaml
id: T-W2-T02
wave: W2
owner_module: tests/acceptance/w2b-math
title: 数学包验证（函数库/双实现验算/目标不变性/等价评分/渲染组件/overlay）
validates: [T-W2-025, T-W2-026, T-W2-027, T-W2-028, T-W2-029, T-W2-030]
prerequisites: [T-W2-027, T-W2-028, T-W2-029, T-W2-030]   # 数学包依赖图终端节点（传递覆盖 025/026 及其上游）
parallel_group: W2b-学科包
model_floor: T1
token_budget: 400k
validation_script:          # 无独立脚本：验收由下方快速验证清单（各卡 tests/unit 单测 + overlay 可解析性检查）承载
```

## 验证策略
验证数学 SubjectPack 的核心能力：安全函数库、双实现独立验算、目标不变性、数学等价评分、静态渲染组件，以及约束 overlay 预设（T-W2-030，仅 YAML，做可解析性检查）。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W2-T02: 数学包验证（函数库/双实现验算/目标不变性/等价评分/渲染组件/overlay） =="
python -m pytest tests/unit/test_math_functions.py -q
python -m pytest tests/unit/test_math_dual_check.py -q
python -m pytest tests/unit/test_math_validator.py -q
python -m pytest tests/unit/test_math_equivalence.py -q
python -m pytest tests/unit/test_math_render_components.py -q
python -c "import yaml; yaml.safe_load(open('src/packs/subject-math/assembly/overlay.yaml', encoding='utf-8'))" \
  && echo "✅ overlay.yaml 可解析（T-W2-030）"
echo "✅ T-W2-T02 验证通过"
```
