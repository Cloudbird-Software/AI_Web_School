# 任务卡
```yaml
id: T-W2-011
wave: W2
title: 门证书签发服务 + 直写 serving 区失败实证
spec: [specs/contracts/db/item-model.md#4, specs/constitution.md#第二部分（D2 门的数据库级强制）, ADR/母题与知识体系-工程架构设计方案v2.md#4.3]
context_paths: [src/core/gate/certifier/, src/core/content/writer.py]
deliverables: [src/core/gate/certifier/service.py, src/core/gate/certifier/serving_views.sql, tests/unit/test_gate_bypass.py]
acceptance: make accept TASK=T-W2-011
accept_script:              # 验收由 tests/unit 单测（test_gate_bypass.py 实证直写失败）+ 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/gate/certifier
depends_on: [T-W2-010, T-W1-007]
non_goals: [完整 RBAC, 审计告警 UI, 灰度发布]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现门证书签发服务：校验全部阻断项通过后签发 GateCertificate，供内容写入服务使用。
创建 serving 视图/角色，只读 published 区；绕过写入服务直写 serving 表/视图在 DB 层失败。
自动化测试实证直写失败。

## 验收标准（逐条可执行）
1. `issue_certificate(...)` 仅在全部阻断项 pass 时生成合法 cert_id，并关联 GateRun。
2. `src/core/gate/certifier/serving_views.sql` 创建只读 serving 视图与角色，禁止 INSERT/UPDATE/DELETE。
3. `tests/unit/test_gate_bypass.py` 用低权限角色直写 serving 视图，断言抛出权限错误。
4. `make accept TASK=T-W2-011` 全绿；E2E-5 通过。
