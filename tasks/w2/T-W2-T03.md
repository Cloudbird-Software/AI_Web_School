# 验证卡
```yaml
id: T-W2-T03
wave: W2
owner_module: tests/acceptance/w2b-render
title: 渲染与追溯验证（Render IR/HTML/PDF/品牌模板/卷码/题短码/周更）
validates: [T-W2-033, T-W2-034, T-W2-035, T-W2-036, T-W2-037, T-W2-038]
prerequisites: [T-W2-038]   # 传递覆盖 033/034/035/036/037 及其上游（门/实例化）
parallel_group: W2b-交付域
model_floor: T1
token_budget: 400k
validation_script: tests/acceptance/w2b-render/test_w2_t03_render_trace.py   # 脚本随本卡验证执行时交付于同路径
```

## 验证策略
验证交付域：Render IR、HTML/CSS 渲染、PDF 导出、品牌模板、paper/paper_item 追溯（卷码 QR/题短码）、周更批处理管线。
T-W2-036（品牌模板）为纯 CSS/HTML 交付，无单测，做文件存在性检查；追溯码端到端（题码→item_version→门证书）由本卡交付的验收脚本承载。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W2-T03: 渲染与追溯验证（Render IR/HTML/PDF/品牌模板/卷码/题短码/周更） =="
docker compose up -d --wait db
python -m pytest tests/unit/test_render_ir.py -q
python -m pytest tests/unit/test_html_renderer.py -q
python -m pytest tests/unit/test_pdf_export.py -q
python -m pytest tests/unit/test_paper_trace.py -q
python -m pytest tests/unit/test_weekly_batch.py -q
# 品牌模板存在性（T-W2-036）
test -f src/core/render/brand/default.css && test -f src/core/render/brand/page.html \
  && echo "✅ 品牌模板文件齐备（T-W2-036）"
# 追溯码端到端（脚本随本卡验证执行时交付于同路径）
python -m pytest tests/acceptance/w2b-render/test_w2_t03_render_trace.py -q
echo "✅ T-W2-T03 验证通过"
```
