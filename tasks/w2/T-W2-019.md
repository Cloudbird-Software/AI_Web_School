# 任务卡
```yaml
id: T-W2-019
wave: W2
title: 测试隔离（事务回滚 / 独立 schema）
spec: [specs/constitution.md#P3, ADR/OPC-AI原生开发组织方案.md#2.2]
context_paths: [tests/conftest.py, tests/unit/test_isolation.py]
deliverables: [tests/conftest.py（事务 fixture）, tests/unit/test_isolation.py]
acceptance: make accept TASK=T-W2-019
accept_script:
model_floor: T1
token_budget: 300k
owner_module: tests/
depends_on: []
non_goals: [生产环境改动, 多数据库分片]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
消除测试数据互染：在 conftest 引入 async 事务回滚或独立测试 schema。
保证单元/集成测试可并行、可重跑，不污染开发数据库。

## 验收标准（逐条可执行）
1. `async_session` fixture 在测试结束后回滚事务。
2. 新增 `test_isolation.py`：测试 A 写入数据，测试 B 在同一进程内看不到。
3. `make test` 在已存在数据的数据库上仍可全绿。
