# 任务卡
```yaml
id: T-W2-029
wave: W2
title: 数学渲染组件（数轴/方格 SVG）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6]
context_paths: [src/packs/subject-math/render/number_line.py, src/packs/subject-math/render/grid.py]
deliverables: [src/packs/subject-math/render/number_line.py, src/packs/subject-math/render/grid.py, tests/unit/test_math_render_components.py]
acceptance: make accept TASK=T-W2-029
accept_script:                  # 置空：tests/acceptance/ 不存在；验收由 tests/unit/test_math_render_components.py + 通用门禁承载
model_floor: T1
token_budget: 300k
owner_module: src/packs/subject-math/render
depends_on: [T-W2-033]
non_goals: [动态交互作图, 在线组件, 3D 图形]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现数学静态卷需要的渲染组件：数轴、方格纸，输出 SVG 嵌入 Render IR。
组件接受参数化配置（范围/刻度/高亮区间）。

## 验收标准（逐条可执行）
1. `number_line.py` 生成 SVG 数轴，含刻度、标签、可选高亮点/区间。
2. `grid.py` 生成方格纸 SVG，可配置行列数与单元格高亮。
3. 输出为 Render IR 的 `math_svg` 块，可被 HTML 渲染器消费。
4. 单元测试覆盖渲染输出校验（关键元素存在）。
