# 任务卡
```yaml
id: T-W2-039
wave: W2
title: FastAPI 只读 API v1（题库/母题/版本/门状态）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#3.2, specs/contracts/api/openapi.yaml]
context_paths: [src/api/main.py, src/api/routers/items.py, src/api/routers/gate.py]
deliverables: [src/api/main.py, src/api/routers/items.py, src/api/routers/gate.py, tests/unit/test_api_readonly.py]
acceptance: make accept TASK=T-W2-039
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/api
depends_on: [T-W2-011]
non_goals: [写入 API, 教研工作台后端, 鉴权 RBAC]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 FastAPI 只读 API v1：查询 item/item_version/item_template/gate_certificate。
OpenAPI 自动产出，与 specs/contracts/api/openapi.yaml 对齐。

## 验收标准（逐条可执行）
1. `GET /items/{item_id}` 返回 item 及其 current_version。
2. `GET /item_versions/{item_version_id}` 返回版本内容与谱系。
3. `GET /gate_certificates/{cert_id}` 返回门证书与 runs。
4. 启动服务后 `/docs` 可访问；产出 OpenAPI 草稿 `src/api/openapi-draft.yaml`（T-W2-040 据此反向生成正式契约并做 diff 测试——终审修：契约由 040 后置产出，本卡不做契约 diff）。
5. 单元测试覆盖 200/404。
