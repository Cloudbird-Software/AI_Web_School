# 任务卡
```yaml
id: T-W2-020
wave: W2
title: pip-compile 重跑与 requirements 头注释对齐
spec: [specs/constitution.md#X8]
context_paths: [pyproject.toml, requirements.txt, requirements-dev.txt]
deliverables: [requirements.txt, requirements-dev.txt]
acceptance: make accept TASK=T-W2-020
accept_script:
model_floor: T2
token_budget: 100k
owner_module: repo-root
depends_on: []
non_goals: [新增业务依赖, 升级主要版本]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
使用 pip-compile 重跑 requirements.txt 与 requirements-dev.txt，确保与 pyproject.toml 一致。
头注释包含生成命令与日期，锁定文件无漂移。

## 验收标准（逐条可执行）
1. `requirements.txt` 头注释注明 `pip-compile` 命令与生成日期。
2. `pyproject.toml` 中所有依赖均在 requirements.txt 中锁定。
3. `make test` 在干净虚拟环境安装后全绿。
