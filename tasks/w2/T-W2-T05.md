# 验证卡
```yaml
id: T-W2-T05
wave: W2
owner_module: tests/acceptance/exit
title: W2 端到端出口验证（E2E-1/2/3/4/5/9/10）
validates: [T-W2-022, T-W2-023, T-W2-024, T-W2-038, T-W2-039, T-W2-040, T-W2-041, T-W2-042, T-W2-043, T-W2-044, T-W2-045]
prerequisites: [T-W2-T01, T-W2-T02, T-W2-T03, T-W2-T04, T-W2-024, T-W2-040, T-W2-045]   # 四张验证卡 + 黄金/契约/出口链终端节点，覆盖即代表 W2 全部完成
parallel_group: W2-出口
model_floor: T1
token_budget: 400k
validation_script: scripts/wave-exit/w2.sh   # 真实路径，由 T-W2-044 交付（对齐 W1 出口卡 T-W01-T04 先例）
```

## 验证策略
W2 最终出口验证：教研工作台签发数学母题 → 实例化 → 过门 → 入库 → 周更静态卷 → PDF 可下载 → 题码可回溯；DSL 实证、黄金回归、语文链路、直写失败、不退化。

出口映射（E2E-1~10 全覆盖）：
- **E2E-1（主链路）/ E2E-9（出口脚本）**：`scripts/wave-exit/w2.sh`（T-W2-044）现场演示，生成真实 PDF 试卷。
- **E2E-2（DSL 实证）**：只读 API 可查谱系与门状态（T-W2-039/040，经 w2.sh 断言）。
- **E2E-3（黄金回归）**：T-W2-022/023/024，`tests/golden/` 全绿（w2.sh 内含全量测试）。
- **E2E-4（语文链路）**：T-W2-T04 已独立验收，出口经 w2.sh 复核。
- **E2E-5（门物理阻断）**：T-W2-T01（test_gate_bypass）已验收，出口经 w2.sh 复核。
- **E2E-6（受控变式证书）**：由 T-W2-T01（test_variation）承载，不重复演示。
- **E2E-7（矩阵实证，降级口径）**：由 T-W2-024 跨交互黄金集 + w2.sh 演示承载。
- **E2E-8（遥测产能）**：T-W2-021 遥测 JSONL + `python tools/opc dashboard` 产能报告。
- **E2E-10（不退化）**：w0.sh / w1.sh 持续全绿 + 全量测试（w2.sh 内含）。
- S10 收尾卡（T-W2-018~021）由 T-W2-T01 与全量测试承载。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W2-T05: W2 端到端出口验证（E2E-1/2/3/4/5/9/10） =="
python tools/opc board
bash scripts/wave-exit/w0.sh
bash scripts/wave-exit/w1.sh
bash scripts/wave-exit/w2.sh
python tools/opc dashboard   # E2E-8 产能报告
echo "✅ T-W2-T05 验证通过"
```
