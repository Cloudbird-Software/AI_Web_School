# 任务卡
```yaml
id: T-W2-016
wave: W2
title: 语料库底座表 + 数学函数库 v1
spec: [specs/contracts/db/item-model.md#2.5, ADR/母题与知识体系-工程架构设计方案v2.md#5.1]
context_paths: [src/core/models/corpus_asset.py, src/core/models/corpus_version.py, src/packs/subject-math/corpora/functions.yaml]
deliverables: [src/core/content/corpus_loader.py, src/packs/subject-math/corpora/functions.yaml, tests/unit/test_corpus_loader.py]
acceptance: make accept TASK=T-W2-016
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/packs/subject-math
depends_on: [T-W2-015]
non_goals: [语文/英语语料库, B 线装配, 语料查重]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
补全语料库加载/校验逻辑，corpus_version 与 material_version 门字段对齐。
建立数学函数库 v1（安全函数白名单），作为 DSL answer_program 的扩展函数源。
函数库版本化，函数签名与定义分离。

## 验收标准（逐条可执行）
1. `corpus_loader.py` 可加载 corpus_asset + corpus_version，校验 license、status、gate_certificate_id。
2. `src/packs/subject-math/corpora/functions.yaml` 定义 ≥10 个数学函数（如 fraction_simplify, unit_convert, gcd, lcm），含签名/版本/安全标记。
3. 函数库加载器返回函数白名单，可被 expr_eval 注册。
4. 单元测试覆盖加载、缺失 license、函数调用。
