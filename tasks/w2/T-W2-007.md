# 任务卡
```yaml
id: T-W2-007
wave: W2
title: 验证器插件统一契约与基类
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.3, specs/contracts/db/item-model.md#4]
context_paths: [src/core/gate/validator.py, src/core/gate/models.py, tests/unit/test_gate_validator_base.py]
deliverables: [src/core/gate/validator.py, tests/unit/test_gate_validator_base.py]
acceptance: make accept TASK=T-W2-007
accept_script:              # 验收由 tests/unit 单测 + 通用门禁承载（tests/acceptance/ 目录不存在，已置空）
model_floor: T0
token_budget: 800k
owner_module: src/core/gate
depends_on: [T-W1-006]
non_goals: [具体学科验证器, 门编排, 策略矩阵, 证书签发]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
定义验证器插件统一契约：`validate(artifact_ref, ctx) -> {verdict, evidence, confidence, validator_id, version, cost}`。
提供抽象基类与注册机制，支持通用验证器与学科包验证器按包 id 注册。
契约字段类型化，证据结构自描述。

## 验收标准（逐条可执行）
1. `src/core/gate/validator.py` 定义 `Validator` 抽象基类与 `ValidatorResult` Pydantic model。
2. `register_validator(pack_id, validator_cls)` 与 `get_validator(pack_id, validator_id)` 可用。
3. 示例验证器（SchemaValidator）可运行并返回 pass/fail/review 之一。
4. 单元测试覆盖基类契约、注册表、返回值 schema 校验。
5. 不 import 任何学科包/学段包。
