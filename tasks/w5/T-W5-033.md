# 任务卡

```yaml
id: T-W5-033
wave: W5-R
title: gate 切 Go 工具链：gofmt/errcheck/race/goleak + baml golden + sqlc diff 进 CI gate
spec: [specs/adr/0004-go-baml-rewrite-and-wave-replan.md#三, tasks/w5/BRIEF.md#S6, .github/workflows/ci.yml]
context_paths: [.github/workflows/, Makefile, tools/, go.mod]
deliverables:
  - ci.yml gate 聚合新增 go-check job（gofmt/errcheck/vet/test -race/goleak）
  - baml golden 快照检查（BAML-1：.baml 变更未过 golden → 红）
  - sqlc diff 检查（SQL-2：生成物漂移 → 红）
  - SQL-1 迁移成对检查脚本
  - Makefile 对应本地目标（开发者可一键复现 gate）
acceptance: make accept TASK=T-W5-033
model_floor: T1
token_budget: 400k
owner_module: .github/workflows/ci.yml, Makefile, tools/
depends_on: [T-W5-030, T-W5-031, T-W5-032]
non_goals:
  - 不移除 Python 检查（双轨期并存，Python 冻结但须保持绿）
  - 不改组织 CI-Workflows 复用工作流（本仓 job 内完成）
  - 不引入新 SaaS/付费工具
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）

languages.yaml 的 GO-1..5 / BAML-1 / SQL-1/2 从"review 纪律"升级为"gate 强制"：对齐组织治理规则映射（ADR-0004 §三）。gate 是唯一 required check，新增检查全部聚合进 gate，不产生第二个 required check。

## 验收标准（逐条可执行）

1. 故意打乱 gofmt → gate 红；恢复 → 绿（GO-1 实证）。
2. 引入未检查 error → errcheck 红（GO-2 实证）。
3. 修改 .baml 不更新 golden 快照 → 红（BAML-1 实证）。
4. 手改 sqlc 生成物 → sqlc diff 红（SQL-2 实证）。
5. 新增 only-up 迁移文件 → SQL-1 成对检查红（实证）。
6. gate 聚合语义不变：任何新增 job 失败 → gate 失败；无"跳过=通过"路径。
