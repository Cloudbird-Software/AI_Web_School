# 任务卡

```yaml
id: T-W5-032
wave: W5-R
title: 迁移移植：alembic 22 个迁移 → db/migrations 纯 SQL up/down + migrate 运行时
spec: [specs/adr/0004-go-baml-rewrite-and-wave-replan.md#四, tasks/w5/REANCHORING.md#二, specs/constitution.md#第二部分]
context_paths: [alembic/versions/, db/migrations/, Makefile, docker-compose.yml]
deliverables:
  - db/migrations/0001..0022_*.up.sql / *.down.sql（22 对，语义对照表见 REANCHORING.md §二）
  - 迁移执行器接入（golang-migrate library 模式或等价，锁版本）
  - make migrate-go / migrate-go-check 目标（up→down→up 全量演练）
  - 移植对照清单勾稽：每个 alembic down 语义在 SQL down 中等价
acceptance: make accept TASK=T-W5-032
model_floor: T1
token_budget: 400k
owner_module: db/, Makefile
depends_on: []
non_goals:
  - 不改任何表语义（只翻译语言，不做 schema 演进）
  - 不删除 alembic/（Go E2E 全绿前保留对照，ADR-0004 §四）
  - 不做数据回填
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）

22 个 alembic 迁移（down 全部非空，审计见 REANCHORING.md §二）翻译为 golang-migrate 纯 SQL 目录，为 Go 服务提供与 Python 版完全同构的 DB 底座。SQL 与语言无关，语义零变更是硬约束。

## 验收标准（逐条可执行）

1. 22 对 up/down 文件齐全（SQL-1 成对性脚本检查零缺失）。
2. 空库 `migrate up` 到 head 成功；`migrate down` 到 0 再 up 到 head 成功（全量可逆，E2E-9 的 Go 侧事实源）。
3. 对照 alembic：`alembic upgrade head` 的 schema 与 `migrate up` 的 schema 逐表逐触发器一致（pg_dump schema diff 为空，忽略迁移工具自带元数据表）。
4. append-only 触发器行为等价：对四张账表 UPDATE/DELETE 被拒（E2E-2 语义，Go 侧测试可执行）。
5. make migrate-go-check 进 CI（PR 阶段拦截，对齐 T-W5-022 语义）。
