# 任务卡
```yaml
id: T-W4-034
wave: W4
title: 扫描件姓名redaction占位 + 时长保护触发
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.8, specs/constitution.md#D8, tasks/w4/BRIEF.md#S7]
context_paths: [src/core/compliance/, src/core/session/]
deliverables: [src/core/compliance/redaction.py, src/core/session/duration_guard.py, tests/unit/test_redaction.py, tests/unit/test_duration_guard.py]
acceptance: make accept TASK=T-W4-034
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/compliance
depends_on: [T-W4-033]
non_goals: [OCR 姓名检测, 自动批改 redaction, 用眼保护提醒 UI, 家长端监控]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现扫描件姓名 redaction（占位符替换，如「[姓名]」）与时长保护触发：低段（1–2）≤15 分钟、中段（3–4）≤60 分钟、高段（5–6）≤60 分钟。超时时长保护触发，阻止新题下发。Redaction 为纯文本替换，不做 OCR。

## 验收标准（逐条可执行）
1. `redact_name(text, name)` 将文本中的姓名替换为「[姓名]」；支持常见姓名变体（无 OCR，纯字符串匹配）。
2. 时长保护：`DurationGuard.check(session, grade_band)` 在超时时返回 False，阻止取下一题。
3. 学段阈值正确：L≤15min / M≤60min / H≤60min；可配置。
4. `make accept TASK=T-W4-034` 全绿。
5. 不 import 任何学科包/学段包。