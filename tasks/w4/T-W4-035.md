# 任务卡
```yaml
id: T-W4-035
wave: W4
title: GradeBandPack L 参数包（注音/朗读/大字号/数字键盘）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#5.3, tasks/w4/BRIEF.md#S8]
context_paths: [src/packs/gradeband_low/, src/core/render/]
deliverables: [src/packs/gradeband_low/config.yaml, src/packs/gradeband_low/render_hints.py, tests/unit/test_gradeband_low.py]
acceptance: make accept TASK=T-W4-035
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/packs/gradeband_low
depends_on: []
non_goals: [中/高学段包, 手写板驱动, 图形化拖拽组件, 闯关游戏引擎]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现低学段包（GradeBandPack L，1–2 年级）完整参数包：全文注音开关、题面朗读按钮、大字号大按钮、数字键盘交互、题量上限、闯关形态标记。参数以 YAML 配置 + Python 渲染提示器形式交付，供渲染与交互层消费。

## 验收标准（逐条可执行）
1. `config.yaml` 含完整参数：font_size_large/phonetic_switch/read_aloud_button/numeric_keyboard/max_items/session_form_game。
2. `render_hints(item, grade_band)` 返回渲染提示 dict（如 `{phonetic: true, font_size: 24px, keyboard: numeric}`）。
3. 低段专属交互：数字键盘仅允许输入 0–9；大字号 ≥20px；注音覆盖全部超纲字。
4. `make accept TASK=T-W4-035` 全绿。
5. 核心域零特判：参数包位于 src/packs/gradeband_low/，核心域通过学段参数注入消费。