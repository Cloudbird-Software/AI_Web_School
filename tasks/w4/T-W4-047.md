# 任务卡
```yaml
id: T-W4-047
wave: W4
title: CTT min_sample门槛(30) + 小样本区分度警示
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.7, tasks/w4/BRIEF.md#S11]
context_paths: [src/core/data/]
deliverables: [src/core/data/ctt.py, tests/unit/test_ctt_min_sample.py]
acceptance: make accept TASK=T-W4-047
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/data
depends_on: []
non_goals: [自适应样本量, 贝叶斯小样本修正, 缺失值插补, 多层模型]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
修复 W3 遗留：CTT 计算增加 min_sample 门槛（默认 30），n<30 时区分度计算返回 None 并标记警示。不修改历史计算逻辑，仅增强边界判断。与既有 CTT 实现向后兼容。

## 验收标准（逐条可执行）
1. `compute_discrimination(responses, key)` 在 n<30 时返回 `None` + 警告日志/标记。
2. n≥30 时计算行为与既有实现完全一致（ golden 数据集回归）。
3. 所有既有 CTT 测试继续通过（不退化）。
4. `make accept TASK=T-W4-047` 全绿。
5. 不 import 任何学科包/学段包。