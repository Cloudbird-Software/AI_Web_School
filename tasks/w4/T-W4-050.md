# 任务卡
```yaml
id: T-W4-050
wave: W4
title: w4.sh 出口脚本 + 不退化验证
spec: [tasks/w4/BRIEF.md#S12, specs/constitution.md#X1, specs/contracts/FROZEN.txt]
context_paths: [scripts/wave-exit/, tests/contract/, tests/golden/]
deliverables: [scripts/wave-exit/w4.sh, scripts/wave-exit/check_no_regression.py]
acceptance: make accept TASK=T-W4-050
accept_script: scripts/wave-exit/w4.sh
model_floor: T1
token_budget: 400k
owner_module: scripts/wave-exit
depends_on: [T-W4-046]
non_goals: [生产部署脚本, 自动发布, 蓝绿部署, 回滚机制]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 W4 出口脚本 w4.sh：执行全部 W4 验证卡快速清单 + W0/W1/W2/W3 出口脚本（不退化） + API 契约 diff 测试 + 学生模拟器全链路。脚本全绿即代表 W4 可出口。脚本由本卡交付，为 T-W4-T05 所引用。

## 验收标准（逐条可执行）
1. `w4.sh` 一键执行：w0.sh → w1.sh → w2.sh → w3.sh → W4 验证清单 → 模拟器场景 → 契约 diff。
2. 任一步骤失败即整体退出码非零，输出失败步骤与日志路径。
3. `check_no_regression.py` 扫描测试总数与通过率，对比基线（W3 出口时的测试数），发现测试减少即失败。
4. `make accept TASK=T-W4-050` 全绿；`bash scripts/wave-exit/w4.sh` 本地可运行（使用 mock/stub 替代外部依赖）。
5. E2E-10（不退化）由本卡承载。