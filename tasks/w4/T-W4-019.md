# 任务卡
```yaml
id: T-W4-019
wave: W4
title: AI维度量规评分器实现（scorer.yaml ai_rubric落地）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.5, specs/contracts/registries/scorer.yaml, tasks/w4/BRIEF.md#S4]
context_paths: [src/core/scoring/, src/core/ai/bus/, src/packs/subject-chinese/]
deliverables: [src/core/scoring/ai_rubric_scorer.py, src/core/scoring/rubric_parser.py, tests/unit/test_ai_rubric_scorer.py]
acceptance: make accept TASK=T-W4-019
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/scoring
depends_on: [T-W4-007, T-W4-017]
non_goals: [自动作文生成, 手写识别评分, 口语量规, 灰度发布控制]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 AI 维度量规评分器：解析量规模板 → 按维度构建评分 prompt → 经 S2 总线 L2/L3 档调用强模型 → 返回逐维分数 + 理由 + 置信度。总分与维度分均返回，不输出排名。契约落地 specs/contracts/registries/scorer.yaml 的 ai_rubric 类型。

## 验收标准（逐条可执行）
1. `score(response_text, rubric_template, grade_band)` 返回 `{dimensions: [{name, score, max, rationale, confidence}], total_score, total_max, overall_confidence}`。
2. 逐维理由非空，置信度 0–1 连续值；低置信度（<0.6）标记待人工复核。
3. AI 调用经 S2 总线（T-W4-007），PII 已剥离（T-W4-008），台账已记录。
4. `make accept TASK=T-W4-019` 全绿；单元测试使用 mock AI 响应验证解析逻辑。
5. 不 import 任何学科包/学段包。