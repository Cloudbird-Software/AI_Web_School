# 验证卡
```yaml
id: T-W4-T02
wave: W4
owner_module: tests/acceptance/w4b
title: W4b C/D线+AI总线验证（S2+S3+S4）
validates: [T-W4-007, T-W4-008, T-W4-009, T-W4-010, T-W4-011, T-W4-012, T-W4-013, T-W4-014, T-W4-015, T-W4-016, T-W4-017, T-W4-018, T-W4-019, T-W4-020, T-W4-021]
prerequisites: [T-W4-011, T-W4-016, T-W4-021]
parallel_group: W4b
model_floor: T1
token_budget: 400k
validation_script:
```

## 验证策略
W4b 子波验证：AI 能力总线（LLM 分级/台账/PII 剥离/适配器/成本/TTS）+ C 线素材工坊（语篇 schema/AI 起草/语篇门/题组编排/C 线端到端）+ D 线命题工坊（蓝图/模板/AI 量规/影子模式/D 线端到端）。确保 AI 调用经总线、PII 已剥离、C/D 线可产出入库。

出口映射（E2E-1~10）：
- **E2E-2（C 线）**：由 T-W4-016（test_c_line_pipeline）承载；语篇→题组→过门→入库全链路断言。
- **E2E-3（D 线）**：由 T-W4-020（test_shadow_mode + shadow_dataset 一致率≥70%）承载；AI 量规影子模式基准验证。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W4-T02: W4b C/D线+AI总线验证 =="
# S2 AI总线
python -m pytest tests/unit/test_ai_router.py -q
python -m pytest tests/unit/test_ai_ledger.py -q
python -m pytest tests/unit/test_pii_filter.py -q
python -m pytest tests/unit/test_ai_adapter.py -q
python -m pytest tests/unit/test_item_lifecycle_cost.py -q
python -m pytest tests/unit/test_tts_router.py -q
# S3 C线
python -m pytest tests/unit/test_passage_schema.py -q
python -m pytest tests/unit/test_passage_generator.py -q
python -m pytest tests/unit/test_difficulty_analyzer.py -q
python -m pytest tests/unit/test_passage_gate.py -q
python -m pytest tests/unit/test_testlet_blueprint.py -q
python -m pytest tests/unit/test_c_line_pipeline.py -q
# S4 D线
python -m pytest tests/unit/test_blueprint_schema.py -q
python -m pytest tests/unit/test_chinese_composition_template.py -q
python -m pytest tests/unit/test_ai_rubric_scorer.py -q
python -m pytest tests/unit/test_shadow_mode.py -q
python -m pytest tests/unit/test_d_line_pipeline.py -q
echo "✅ T-W4-T02 验证通过"
```