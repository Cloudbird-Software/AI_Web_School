# 任务卡
```yaml
id: T-W4-009
wave: W4
title: LiteLLM兼容层 / DeepSeek直连适配器
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.8, tasks/w4/BRIEF.md#S2]
context_paths: [src/core/ai/adapter/, src/core/ai/bus/]
deliverables: [src/core/ai/adapter/litellm_client.py, src/core/ai/adapter/deepseek_client.py, src/core/ai/adapter/fallback.py, tests/unit/test_ai_adapter.py]
acceptance: make accept TASK=T-W4-009
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/ai/adapter
depends_on: [T-W4-007]
non_goals: [多模态模型适配, 嵌入模型服务, ASR 服务, 模型微调管线]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 LLM 供应商适配层：LiteLLM 兼容接口（统一 OpenAI 格式）与 DeepSeek 直连客户端（已有 key），两者互备。配置驱动（环境变量 + policy.yaml），支持温度/最大 token/重试策略。 fallback 机制在主供应商失败时自动切换。

## 验收标准（逐条可执行）
1. `LiteLLMClient.complete(prompt)` 与 `DeepSeekClient.complete(prompt)` 返回统一格式（content/usage/model）。
2. Fallback：主客户端失败（超时/5xx/配额）后自动切备客户端，返回结果标记 fallback 标志。
3. 配置热加载：policy.yaml 变更后无需重启即可生效（或最小化重启）。
4. `make accept TASK=T-W4-009` 全绿；单元测试使用 mock 供应商（不消耗真实 API）。
5. 不 import 任何学科包/学段包。