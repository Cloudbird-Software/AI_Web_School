# 任务卡
```yaml
id: T-W4-037
wave: W4
title: 在线UI学段适配层（渲染+交互组件）
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#5.3, tasks/w4/BRIEF.md#S8]
context_paths: [src/core/render/, src/packs/gradeband_low/]
deliverables: [src/core/render/gradeband_adapter.py, src/core/render/components/phonetic_overlay.py, tests/unit/test_gradeband_adapter.py]
acceptance: make accept TASK=T-W4-037
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/render
depends_on: [T-W4-036]
non_goals: [小程序组件, Web 前端框架, 动画引擎, 主题皮肤系统]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现在线渲染学段适配层：Render IR → 按学段包参数生成差异化 HTML/CSS/交互提示。低段自动注入注音层、朗读按钮、大字号、数字键盘；中段/高段按各自参数渲染。适配层与学科渲染组件正交。

## 验收标准（逐条可执行）
1. `adapt_for_gradeband(render_ir, grade_band)` 返回适配后的 Render IR，含学段专属标记与样式提示。
2. 低段渲染输出含：`<ruby>` 注音标签、朗读按钮 data 属性、大字号 CSS 类、数字键盘触发标记。
3. 中段/高段渲染不注入低段专属元素，保持常规呈现。
4. `make accept TASK=T-W4-037` 全绿。
5. 不 import 学科包；学段参数通过 grade_band 配置注入。