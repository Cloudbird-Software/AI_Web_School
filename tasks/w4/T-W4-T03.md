# 验证卡
```yaml
id: T-W4-T03
wave: W4
owner_module: tests/acceptance/w4c
title: W4c 听力+测量验证（S5+S6）
validates: [T-W4-022, T-W4-023, T-W4-024, T-W4-025, T-W4-026, T-W4-027, T-W4-028, T-W4-029, T-W4-030]
prerequisites: [T-W4-026, T-W4-030]
parallel_group: W4c
model_floor: T1
token_budget: 400k
validation_script:
```

## 验证策略
W4c 子波验证：英语听力（TTS 产线/音频门/消费层/组卷 overlay/端到端）+ 测量 v1（细目表/CP-SAT/测量卷/CTT 报告）。确保听力卷可产、测量组卷可行、信度报告可出。

出口映射（E2E-1~10）：
- **E2E-4（听力）**：由 T-W4-026（test_listening_e2e）承载；TTS→音频门→组卷→渲染全链路 + 播放器限次/二维码/点读。
- **E2E-5（测量）**：由 T-W4-028（test_cpsat_solver）承载；可行/不可行细目表均返回正确结果。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W4-T03: W4c 听力+测量验证 =="
# S5 听力
python -m pytest tests/unit/test_audio_producer.py -q
python -m pytest tests/unit/test_audio_gate.py -q
python -m pytest tests/unit/test_audio_consumer.py -q
python -m pytest tests/unit/test_listening_overlay.py -q
python -m pytest tests/unit/test_listening_e2e.py -q
# S6 测量
python -m pytest tests/unit/test_spec_table.py -q
python -m pytest tests/unit/test_cpsat_solver.py -q
python -m pytest tests/unit/test_measurement_paper.py -q
python -m pytest tests/unit/test_ctt_report.py -q
echo "✅ T-W4-T03 验证通过"
```