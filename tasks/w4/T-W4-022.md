# 任务卡
```yaml
id: T-W4-022
wave: W4
title: TTS音频产线 + 内容寻址版本化
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6, specs/constitution.md#D3, tasks/w4/BRIEF.md#S5]
context_paths: [src/core/audio/, src/core/ai/tts/]
deliverables: [src/core/audio/producer.py, src/core/audio/content_addressing.py, tests/unit/test_audio_producer.py]
acceptance: make accept TASK=T-W4-022
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/audio
depends_on: [T-W4-011]
non_goals: [音频编辑工作站, 多轨混音, 背景音乐合成, 实时语音合成]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 TTS 音频产线：经 S2 TTS 总线合成听力素材音频，输出内容寻址 id（H(text, voice_profile, speed, engine_digest)），版本化管理。支持批量生产任务队列。音频文件入对象存储，元数据入素材库。

## 验收标准（逐条可执行）
1. `produce_audio(text, voice_profile, grade_band)` 返回音频素材对象（url/content_hash/duration/tts_metadata）。
2. 内容寻址 id 按公式计算：相同输入返回相同 id；任何参数变更产生新 id。
3. 批量生产：`produce_audio_batch(jobs)` 异步处理，返回任务状态与结果列表。
4. `make accept TASK=T-W4-022` 全绿；单元测试使用 mock TTS。
5. 不 import 任何学科包/学段包。