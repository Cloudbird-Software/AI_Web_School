# 验证卡
```yaml
id: T-W4-T01
wave: W4
owner_module: tests/acceptance/w4a
title: W4a 数据域+合规验证（S1+S7）
validates: [T-W4-001, T-W4-002, T-W4-003, T-W4-004, T-W4-005, T-W4-006, T-W4-031, T-W4-032, T-W4-033, T-W4-034]
prerequisites: [T-W4-003, T-W4-005, T-W4-006, T-W4-032, T-W4-034]
parallel_group: W4a
model_floor: T1
token_budget: 400k
validation_script:
```

## 验证策略
W4a 子波验证：数据域完整化（贝叶斯收缩/ActiveModelPointer/重放/健康度/缺口盘点/Parquet 归档）+ 合规层（PII 保险库/家长授权/无排名实证/时长保护）。确保数据飞轮可运转且合规项机器可验。

出口映射（E2E-1~10）：
- **E2E-6（合规）**：由 T-W4-033（test_no_ranking_query + check_no_ranking.py）承载；T-W4-031（PII 保险库 schema）+ T-W4-032（家长授权）+ T-W4-034（时长保护）复核。
- **E2E-8（重放-数据域部分）**：由 T-W4-003（annual_replay.py）承载；T-W4-046 在 W4d 中做完整首演报告。
- **E2E-9（融合策略）**：由 T-W4-001（test_bayesian_shrinkage 三档实证）承载。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W4-T01: W4a 数据域+合规验证 =="
# S1 数据域
python -m pytest tests/unit/test_bayesian_shrinkage.py -q
python -m pytest tests/unit/test_active_model_pointer.py -q
python -m pytest tests/unit/test_replay.py -q
python -m pytest tests/unit/test_item_health.py -q
python -m pytest tests/unit/test_coverage_gap.py -q
python -m pytest tests/unit/test_parquet_export.py -q
# S7 合规层
python -m pytest tests/unit/test_pii_vault.py -q
python -m pytest tests/unit/test_parental_consent.py -q
python -m pytest tests/contract/test_no_ranking_query.py -q
python -m pytest tests/unit/test_redaction.py -q
python -m pytest tests/unit/test_duration_guard.py -q
bash scripts/ci/check_no_ranking.py
echo "✅ T-W4-T01 验证通过"
```