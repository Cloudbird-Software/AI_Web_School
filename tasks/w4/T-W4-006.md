# 任务卡
```yaml
id: T-W4-006
wave: W4
title: response_event Parquet 归档增强 + 分区策略
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.7, specs/contracts/events/response_event.md, tasks/w4/BRIEF.md#S1]
context_paths: [src/core/events/, src/core/data/]
deliverables: [src/core/data/parquet_export.py, scripts/jobs/daily_parquet_export.py, tests/unit/test_parquet_export.py]
acceptance: make accept TASK=T-W4-006
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/data
depends_on: []
non_goals: [对象存储生命周期管理, 实时流式导出, 数据仓库对接, 增量压缩]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 response_event 每日增量导出 Parquet 至对象存储：按月分区、按场景（practice/diagnosis/measurement）分文件、含完整字段（含 testlet_id、播放行为等 W4 新增字段）。导出脚本可定时执行，支持幂等重跑。

## 验收标准（逐条可执行）
1. `daily_parquet_export.py` 按日期分区导出前一日增量，输出路径含日期与场景标记。
2. Parquet 文件 schema 与 response_event 全字段对齐，含 testlet_id / playback_events / dimension_scores 等 W4 字段。
3. 幂等重跑：同一日期多次执行产出相同文件（去重键防重复写入）。
4. `make accept TASK=T-W4-006` 全绿。
5. 不 import 任何学科包/学段包。