# 任务卡
```yaml
id: T-W4-007
wave: W4
title: AI总线核心框架 + LLM分级调用契约
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.8, specs/constitution.md#X6, tasks/w4/BRIEF.md#S2]
context_paths: [src/core/ai/, specs/contracts/registries/]
deliverables: [src/core/ai/bus/router.py, src/core/ai/bus/models.py, src/core/ai/bus/policy.yaml, tests/unit/test_ai_router.py]
acceptance: make accept TASK=T-W4-007
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/ai/bus
depends_on: []
non_goals: [供应商自动发现, 负载均衡集群, 流式响应代理, prompt 工程平台]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 AI 能力总线核心：LLM 统一收口，按任务分级（L0 不用 AI / L1 弱模型 / L2 强模型 / L3 复核）。供应商可配置（LiteLLM 兼容层或直连），关键任务双供应商预案。所有 AI 调用经总线，禁止各域直连供应商。

## 验收标准（逐条可执行）
1. `ai_call(task_level, prompt, context)` 按 policy.yaml 路由到对应模型；L0 直接返回空/拒绝；L1/L2/L3 分别命中不同模型配置。
2. 调用返回结构化结果（内容 + token 用量 + 耗时 + 模型标识），异常时走 fallback 供应商。
3. 核心域代码 import 路径验证：任何 src/core/<非 ai>/ 代码不得直接 import openai/deepseek/anthropic（CI 静态扫描）。
4. `make accept TASK=T-W4-007` 全绿。
5. 不 import 任何学科包/学段包。