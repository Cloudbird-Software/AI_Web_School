# 任务卡
```yaml
id: T-W4-008
wave: W4
title: LLM调用台账 + PII剥离中间件
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.8, specs/constitution.md#D7, tasks/w4/BRIEF.md#S2]
context_paths: [src/core/ai/, src/core/models/]
deliverables: [src/core/ai/ledger/ledger.py, src/core/ai/ledger/pii_filter.py, src/core/ai/ledger/schemas.py, tests/unit/test_ai_ledger.py, tests/unit/test_pii_filter.py]
acceptance: make accept TASK=T-W4-008
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/ai/ledger
depends_on: [T-W4-007]
non_goals: [完整审计 UI, 实时成本告警, 数据血缘图谱, 法务合规模块]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 AI 调用台账与 PII 剥离：每次调用记录任务/模型/prompt 版本/token/成本/关联产物 id；LLM/TTS 调用前自动剥离 PII（student_alias_id 替换为临时令牌，姓名/电话/地址泛化）。台账 append-only，供成本归集与审计。

## 验收标准（逐条可执行）
1. `record_call(...)` 写入台账表/文件，字段完整（任务/模型/prompt_hash/token_in/token_out/cost_cny/artifact_ref）。
2. `pii_filter.strip(text)` 输出无 PII 文本：学生姓名替换为「学生A」、电话替换为「[PHONE]」、地址泛化；student_alias_id 保留（非 PII）。
3. 台账查询支持按 item_revision 归集单题全生命周期 AI 成本（T-W4-010 消费此接口）。
4. `make accept TASK=T-W4-008` 全绿；PII 剥离测试含 5 类以上 PII 样本。
5. 不 import 任何学科包/学段包。