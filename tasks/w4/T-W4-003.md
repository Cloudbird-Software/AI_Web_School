# 任务卡
```yaml
id: T-W4-003
wave: W4
title: 增量重判框架 + 年度全量重放演练脚本
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.7, specs/constitution.md#D6, tasks/w4/BRIEF.md#S1]
context_paths: [src/core/data/, src/core/scoring/, src/core/events/]
deliverables: [src/core/data/replay.py, scripts/jobs/annual_replay.py, tests/unit/test_replay.py]
acceptance: make accept TASK=T-W4-003
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/data
depends_on: [T-W4-002]
non_goals: [实时重判, 生产环境自动触发, 回滚历史分数, 异步流处理]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现增量重判与年度全量重放：新 scorer 上线时，仅对新增/变更范围写平行 score_run，原序列不动；每年一次全量重放演练脚本制度化。演练输出新旧参数并存报告，证明"可重算"。

## 验收标准（逐条可执行）
1. `incremental_rescore(item_ids, new_scorer_version)` 为指定题生成平行 score_run，原 score_run 不受影响。
2. `annual_replay.py` 脚本：读取全部历史 response_event，用当前活跃估计器重算，输出新旧对比报告（参数差异分布、一致性率）。
3. 重放结果 100% 可复现：同一代码版本 + 同数据快照输出一致（摘要哈希比对）。
4. `make accept TASK=T-W4-003` 全绿；`python scripts/jobs/annual_replay.py --dry-run` 无报错。
5. 不 import 任何学科包/学段包。