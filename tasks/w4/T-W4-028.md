# 任务卡
```yaml
id: T-W4-028
wave: W4
title: OR-Tools CP-SAT离线求解器接入
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.4, tasks/w4/BRIEF.md#S6]
context_paths: [src/core/assembly/, src/core/assembly/solver/]
deliverables: [src/core/assembly/solver/cpsat_solver.py, src/core/assembly/solver/constraints.py, tests/unit/test_cpsat_solver.py]
acceptance: make accept TASK=T-W4-028
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/assembly/solver
depends_on: [T-W4-027]
non_goals: [在线实时求解, 多目标帕累托优化, 求解器自动选型, 分布式求解]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
接入 OR-Tools CP-SAT 作为离线组卷求解器：将双向细目表 + 候选题池 + 附加约束（曝光互斥/题组/testlet）编译为 CP-SAT 模型，求解可行解或返回不可行原因（冲突约束列表）。确定性：同输入同种子同输出。

## 验收标准（逐条可执行）
1. `solve(spec_table, candidate_pool, seed=0)` 返回题卷列表或 `Infeasible` 对象（含冲突约束列表）。
2. 可行案例：2 知识点×2 认知层级×难度配比 = 8 单元格共 12 题，求解成功且每单元格题数与难度合规。
3. 不可行案例：题池不足或约束冲突时返回明确冲突原因（如"单元格 A 需 3 题但候选仅 2 题"）。
4. `make accept TASK=T-W4-028` 全绿；E2E-5 承载卡。
5. 不 import 任何学科包/学段包。