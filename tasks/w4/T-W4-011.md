# 任务卡
```yaml
id: T-W4-011
wave: W4
title: TTS总线档 + 多音色语速学段配置
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.6, ADR/母题与知识体系-工程架构设计方案v2.md#4.8, tasks/w4/BRIEF.md#S2, tasks/w4/BRIEF.md#S5]
context_paths: [src/core/ai/tts/, src/packs/subject-english/]
deliverables: [src/core/ai/tts/router.py, src/core/ai/tts/voice_profiles.yaml, tests/unit/test_tts_router.py]
acceptance: make accept TASK=T-W4-011
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/ai/tts
depends_on: [T-W4-007]
non_goals: [ASR 口语评分, 实时语音对话, 情感合成, 歌声合成]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 TTS 总线档：经 AI 总线统一调用，支持多音色、语速按学段配置（低段慢速/高段常速）。输出音频素材内容寻址、版本化。为 S5 英语听力产线提供语音合成服务。

## 验收标准（逐条可执行）
1. `tts_synthesize(text, grade_band, voice_profile)` 返回音频字节流 + 内容寻址 id + 元数据（语速/音色/时长）。
2. 学段配置：L(1–2) 慢速(120wpm)/M(3–4) 中速(140wpm)/H(5–6) 常速(160wpm) 可配置。
3. 相同文本+相同配置返回相同内容寻址 id（缓存命中）。
4. `make accept TASK=T-W4-011` 全绿；单元测试使用 mock TTS 引擎。
5. 不 import 学科包；学段参数通过配置注入。