# 任务卡
```yaml
id: T-W4-013
wave: W4
title: AI起草语篇(L2档) + 语篇难度分析
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.1, ADR/母题与知识体系-工程架构设计方案v2.md#4.8, tasks/w4/BRIEF.md#S3]
context_paths: [src/core/content/, src/core/ai/bus/]
deliverables: [src/core/content/passage_generator.py, src/core/content/difficulty_analyzer.py, tests/unit/test_passage_generator.py, tests/unit/test_difficulty_analyzer.py]
acceptance: make accept TASK=T-W4-013
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/content
depends_on: [T-W4-007, T-W4-012]
non_goals: [L3 复核级生成, 多轮对话改写, 自动插图生成, 语篇摘要]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 AI 语篇起草与难度分析：按命题方向（知识点×体裁×难度×学段）经 S2 AI 总线 L2 档生成语篇草稿；自动分析字频/句长/生词率，输出难度指标。草稿标记 AI 生成来源，待教研改写。

## 验收标准（逐条可执行）
1. `generate_passage(prompt_direction, grade_band)` 经 ai_call(L2, ...) 返回语篇正文 + 生成元数据（模型/prompt_hash/token）。
2. `analyze_difficulty(text, grade_band)` 返回字频分布、平均句长、生词率（相对课标词表），与预设难度区间比对给出偏差报告。
3. AI 调用经 S2 总线（T-W4-007），调用记录入台账（T-W4-008）。
4. `make accept TASK=T-W4-013` 全绿；单元测试使用 mock AI 响应。
5. 不 import 任何学科包/学段包。