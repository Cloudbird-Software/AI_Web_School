# 任务卡
```yaml
id: T-W4-048
wave: W4
title: 评分器补写 scoring_trace.process.correct 键
spec: [specs/contracts/events/response_event.md, tasks/w4/BRIEF.md#S11]
context_paths: [src/core/scoring/]
deliverables: [src/core/scoring/service.py, tests/unit/test_scoring_trace_correct.py]
acceptance: make accept TASK=T-W4-048
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/scoring
depends_on: []
non_goals: [评分轨迹可视化, 错误推断增强, 评分器市场, 实时评分流]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
修复 W3 遗留：评分器在写入 response_event 时补全 scoring_trace.process.correct 键（布尔值，标识最终判定正误）。复习排程推进依赖此键，缺失会导致复习队列逻辑异常。

## 验收标准（逐条可执行）
1. 全部现役评分器（精确匹配/数学等价/ai_rubric 等）输出 scoring_trace 含 `process.correct` 布尔值。
2. `response_event` 写入后，查询可确认 `scoring_trace.process.correct` 存在且非空。
3. 既有评分测试通过（不退化）；新增测试覆盖全部评分器类型的 correct 键。
4. `make accept TASK=T-W4-048` 全绿。
5. 不 import 任何学科包/学段包。