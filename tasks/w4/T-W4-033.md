# 任务卡
```yaml
id: T-W4-033
wave: W4
title: 跨用户排名查询路径不存在静态实证
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.8, specs/constitution.md#D8, tasks/w4/BRIEF.md#S7]
context_paths: [src/core/, tests/contract/, tests/unit/]
deliverables: [tests/contract/test_no_ranking_query.py, scripts/ci/check_no_ranking.py, tests/unit/test_compliance_ranking.py]
acceptance: make accept TASK=T-W4-033
accept_script:
model_floor: T1
token_budget: 400k
owner_module: tests/contract
depends_on: []
non_goals: [运行时监控, 排名功能开关, 等级化呈现 UI, 学生画像]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现"跨用户排名查询路径不存在"的静态实证：代码全仓扫描确认无任何按成绩排序跨用户查询的 SQL/API/函数；CI 静态扫描强制阻断；契约测试断言排名查询返回 404/禁止。E2E-6 核心承载。

## 验收标准（逐条可执行）
1. `scripts/ci/check_no_ranking.py` 扫描 src/ 全部代码，发现任何含 "ORDER BY score" / "rank" / "RANK()" 跨用户的查询即报错退出。
2. 契约测试 `test_no_ranking_query.py` 尝试调用排名接口，断言返回 404 或权限拒绝。
3. 扫描范围覆盖 SQL 字符串、ORM 查询、API 端点定义、报告生成代码。
4. `make accept TASK=T-W4-033` 全绿；E2E-6 承载卡。
5. 不引入假阳性：合法的单用户内排序（如个人历史成绩时间序）不被误报。