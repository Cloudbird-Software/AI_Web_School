# 验证卡
```yaml
id: T-W4-T04
wave: W4
owner_module: tests/acceptance/w4d
title: W4d 性能+模拟器+重放+低学段验证（S8+S9+S10）
validates: [T-W4-035, T-W4-036, T-W4-037, T-W4-038, T-W4-039, T-W4-040, T-W4-041, T-W4-042, T-W4-043, T-W4-044, T-W4-045, T-W4-046]
prerequisites: [T-W4-037, T-W4-041, T-W4-046]
parallel_group: W4d
model_floor: T1
token_budget: 400k
validation_script:
```

## 验证策略
W4d 子波验证：低学段包完整化（GradeBandPack L/约束/渲染适配）+ 性能与运维（压测/备份/成本仪表盘）+ API 冻结/学生模拟器/重放首演。确保发布前就绪清单的机器可验部分全过。

出口映射（E2E-1~10）：
- **E2E-1（学生模拟器全链路）**：由 T-W4-044（practice_scenario）+ T-W4-045（diagnosis+review）承载；模拟器作为 OpenAPI 参考客户端走通全部 C 端链路。
- **E2E-7（性能）**：由 T-W4-038（test_assembly_latency p95<2s）+ T-W4-039（test_grading_latency 10s 级）承载。
- **E2E-8（重放）**：由 T-W4-046（annual_replay_report.py）承载；ActiveModelPointer 切换前后报告各引用当时版本。
- **E2E-10（不退化-API 冻结部分）**：由 T-W4-042（test_api_frozen + check_openapi_diff）承载。

## 快速验证清单（一键执行）

```bash
set -e
echo "== T-W4-T04: W4d 性能+模拟器+重放+低学段验证 =="
# S8 低学段包
python -m pytest tests/unit/test_gradeband_low.py -q
python -m pytest tests/unit/test_gradeband_constraints.py -q
python -m pytest tests/unit/test_gradeband_adapter.py -q
# S9 性能与运维
python -m pytest tests/performance/test_assembly_latency.py -q
python -m pytest tests/performance/test_preassembled_fallback.py -q
python -m pytest tests/performance/test_grading_latency.py -q
bash scripts/ops/backup.sh --dry-run
python scripts/ops/backup_verify.py --check-last
python -m pytest tests/unit/test_cost_dashboard.py -q
# S10 API+模拟器+重放
python -m pytest tests/contract/test_api_frozen.py -q
python scripts/ci/check_openapi_diff.py
python -m pytest tests/unit/test_simulator_client.py -q
python -m pytest tests/simulator/test_practice_e2e.py -q
python -m pytest tests/simulator/test_diagnosis_review_e2e.py -q
python scripts/jobs/annual_replay_report.py --dry-run
echo "✅ T-W4-T04 验证通过"
```