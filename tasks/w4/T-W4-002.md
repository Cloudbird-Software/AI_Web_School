# 任务卡
```yaml
id: T-W4-002
wave: W4
title: ActiveModelPointer 估计器版本切换
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#4.7, specs/constitution.md#D6, tasks/w4/BRIEF.md#S1]
context_paths: [src/core/data/, src/core/models/]
deliverables: [src/core/data/active_model_pointer.py, src/core/models/estimator_run.py, tests/unit/test_active_model_pointer.py]
acceptance: make accept TASK=T-W4-002
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/data
depends_on: [T-W4-001]
non_goals: [在线实时切换, 估计器自动训练, 模型市场, A/B 测试框架]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现 ActiveModelPointer：估计器版本切换机制。每次估计运行绑定 model_version + 代码 digest + 输入数据快照 + 图谱 release；历史报告永远引用当时版本。支持 v1(CTT+经验贝叶斯) 与 v2(Rasch/2PL) 指针登记与切换。

## 验收标准（逐条可执行）
1. `ActiveModelPointer.set_active(purpose_scope, model_version)` 登记当前活跃版本，旧版本记录退役时间戳。
2. `get_params(item_id, timestamp=None)` 按时间戳返回对应版本的参数；无 timestamp 返回当前活跃版本。
3. 同一样本经 v1/v2 估计后分别存储，查询互不影响；历史报告引用当时版本实证。
4. `make accept TASK=T-W4-002` 全绿。
5. 不 import 任何学科包/学段包。