# 任务卡
```yaml
id: T-W4-036
wave: W4
title: 低段题量上限/闯关形态/会话时长约束
spec: [ADR/母题与知识体系-工程架构设计方案v2.md#5.3, tasks/w4/BRIEF.md#S8]
context_paths: [src/core/assembly/, src/core/session/, src/packs/gradeband_low/]
deliverables: [src/core/assembly/gradeband_constraints.py, src/core/session/game_session.py, tests/unit/test_gradeband_constraints.py]
acceptance: make accept TASK=T-W4-036
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/assembly
depends_on: [T-W4-035]
non_goals: [中/高学段约束, 自适应题量, 游戏化动画, 社交排行榜]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
实现低段组卷 overlay 与会话约束：题量上限（低段每卷≤10 题）、闯关形态（即时反馈+星级+下一关解锁）、会话时长硬约束（≤15 分钟）。约束与现有 AssemblyProfile 编译器集成，不可行返回冲突。

## 验收标准（逐条可执行）
1. `apply_gradeband_overlay(paper_spec, grade_band)` 注入学段约束：L 段题量≤10、时长≤15min、形态=闯关。
2. 闯关形态：`game_session.py` 实现关卡状态机（未开始/进行中/完成/星级评定），支持即时反馈。
3. 约束不可行时（如请求 20 题低段卷）返回明确冲突原因。
4. `make accept TASK=T-W4-036` 全绿。
5. 不 import 任何学科包/学段包。