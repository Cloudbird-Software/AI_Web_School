# 任务卡
```yaml
id: T-W0-009
wave: W0
title: 组织治理基线接入（CI-Workflows gate / automerge / scorecard / CODEOWNERS / dependabot）+ main 红灯前置修复
spec: [specs/constitution.md#P3]
context_paths: [.github/workflows/, Makefile, requirements.txt, tests/unit/test_session_service.py]
deliverables: [ci.yml（CI-Workflows@v1 调用 + 宪法门禁迁移）, automerge.yml, scorecard.yml, .github/CODEOWNERS, .github/dependabot.yml, zizmor.yml, .gitleaks.toml, Makefile setup/check, pr-check.yml 并入 ci.yml, cryptography==50.0.0, 分区时间锚点动态化]
acceptance: PR 上 gate 聚合 check 全绿；合并后组织 drift-check 无漂移（ADR-0023）
accept_script:
model_floor: T1
token_budget: 80k
owner_module: .github/
depends_on: [T-W0-008]
non_goals: [语言栈迁移（Python 存量栈按 ADR-0023 存量豁免组织新服务语言准入）, nightly w2.sh playwright 出口失败修复（另行任务）, 业务功能变更]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明
按组织治理仓（.github / template-service / CI-Workflows）要求把本历史仓从 `status: exempt` 纳入治理基线：CI-1（PR 聚合 gate=唯一 required check）、CI-2/3/4（actions 白名单/只读 token/zizmor 审计）、SC-1/2（CodeQL 已于 2026-08-18 启用）、SC-3（dependabot+automerge）、CG-2（CODEOWNERS）、BP-4（仓库设置线上已合规）。前置修复 main 红灯：①test_session_service 时间锚点跨月分区炸弹；②cryptography 49.0.0→50.0.0（PYSEC-2026-3552，pip-audit 门禁清障）。

## 验收标准（逐条可执行）
1. PR 上 `gate` 聚合 check 全绿（hygiene: gitleaks 全历史+zizmor medium+；check: make setup/check；deps: dependency-review；repo-gate: 任务卡/泄密/反削弱/核心域隔离/规则同步/来源登记）
2. `make setup && make check` 与原 pr-check.yml 流水等价（本地可复现）
3. gitleaks 全历史扫描除 .gitleaks.toml 登记的 2 个合成测试常量外零命中
4. ADR-0023 合并后：REPOS.yaml 本仓转 active，组织 drift-check 全绿
