# 任务卡
```yaml
id: T-W0-008
wave: W0
title: W0 反馈修正（最小链路真实化 + 镜像 digest 锁定 + dev 依赖锁定）
spec: [specs/constitution.md#P3]
context_paths: [scripts/wave-exit/w0.sh, docker-compose.yml, requirements-dev.txt]
deliverables: [scripts/demo-w0-min-link.py（建题→过门→查询真实链路）, docker-compose.yml 镜像 digest 锁定, requirements-dev.txt 版本锁定, w0.sh 第 7 项接入真实链路]
acceptance: make accept TASK=T-W0-008
accept_script:
model_floor: T1
token_budget: 200k
owner_module: infra/
depends_on: [T-W0-007]
non_goals: [Chromium/SymPy 依赖（W2/W3 引入，已在 bootstrap-notes 登记）, 生产 requirements.txt（T-W1-001）]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明
回应 W0 反馈：①OPC §6.1 出口演示"创建一条题目记录→过门（占位验证器）→查询"原为 pytest 占位，补真实最小链路（基于 0001 占位 item 表）；②docker-compose 镜像浮动标签固定为 digest（本地与 CI 一致）；③requirements-dev.txt 锁定版本。

## 验收标准（逐条可执行）
1. `python scripts/demo-w0-min-link.py` 输出链路 PASS（INSERT→门校验→SELECT 真实往返）
2. `make demo-w0` 全绿（第 7 项含真实链路）
3. docker-compose.yml 三个镜像均带 sha256 digest 且 `docker compose up -d --wait` 正常
4. requirements-dev.txt 全部 `==` 锁定且 `pip install -r` 可复现
