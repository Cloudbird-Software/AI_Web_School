# 任务卡
```yaml
id: T-W0-007
wave: W0
title: CI 门禁缺陷修复（泄密扫描引号盲区 + tests/unit 纳管 + contract-watch 新增豁免）
spec: [specs/constitution.md#X1, specs/constitution.md#X3, specs/constitution.md#P5]
context_paths: [.github/workflows/, tools/make_accept.sh]
deliverables: [pr-check.yml 正则允许可选引号, pr-check/make_accept 纳入 tests/unit, contract.yml 冻结检查加 --diff-filter=M, 验证 PR 全绿]
acceptance: make accept TASK=T-W0-007
accept_script:
model_floor: T1
token_budget: 200k
owner_module: infra/ci
depends_on: []
non_goals: [workflow 结构调整, 新增门禁]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明
W0 检查中发现三处门禁缺陷：①pr-check 泄密扫描正则不匹配带引号的密钥字面量（`api_key = "sk-..."` 漏检）；②`tests/unit` 不在任何门禁的 pytest 路径（T-W0-003 的单测 CI 永不执行）；③contract-watch 的 `git diff --quiet` 无法区分「新增登记」与「修改冻结文件」，导致契约登记 PR（T-W0-004）被自咬拦红。

## 验收标准（逐条可执行）
1. 含 `api_key = "sk-fake1234567890abcdef123456"` 的演示 PR 被泄密扫描拦红
2. `make accept TASK=T-W0-003` 在合并后真实执行 tests/unit 用例
3. T-W0-004 契约登记 PR 的 contract-watch 转绿（新增不拦、修改仍拦）
