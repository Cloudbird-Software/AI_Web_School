# 任务卡

```yaml
id: T-W0-010
wave: W0
title: 消除仓库全部 19 个 security 警报（CodeQL×4 + Scorecard×15）
spec: [AGENTS.md#代码与测试, specs/adr/0003-vision-v2-and-wave-replan.md#六]
context_paths: [src/workbench/, src/core/monitoring/, .github/workflows/, tests/unit/test_workbench_pages.py, tests/unit/test_cost_dashboard.py]
deliverables:
  - src/workbench/main.py（next 白名单化 + cookie 值服务端派生）
  - src/workbench/auth.py（session_cookie_value 派生与会话校验改造）
  - src/core/monitoring/health_endpoints.py（/health 响应脱敏 + 服务端日志留痕）
  - requirements.txt / requirements-dev.txt（--generate-hashes 重锁）
  - .github/workflows/{ci,contract,nightly}.yml（依赖钉扎 + --require-hashes）
  - LICENSE（Apache-2.0，新增）
  - tools/fuzz/fuzz_pii_redaction.py（atheris harness，新增）
acceptance: make accept TASK=T-W0-010
model_floor: T1
token_budget: 400k
owner_module: .github/, tools/fuzz/, LICENSE, requirements*.txt, src/workbench/, src/core/monitoring/
depends_on: []
non_goals:
  - 不改任何既有测试与断言（X1）
  - 不引入运行时新依赖（atheris 仅本地 fuzz 工具，不进锁定文件）
  - 不新增仓库级 CodeQL 工作流（组织已于 2026-08-18 启用 default setup 且
    覆盖 PR 分析——"Analyze (python)/(actions)" 即其产物；仓库级高级配置与
    default setup 的 SARIF 处理互斥，GitHub 会拒绝："CodeQL analyses from
    advanced configurations cannot be processed when the default setup is
    enabled"）
  - 不做 W5-R Go 重写（本卡只处理 Python 存量警报）
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）

仓库 security 面板存在 19 个开放警报：CodeQL 4 个（open redirect ×2 / cookie injection / stack trace exposure）+ Scorecard 15 个（PinnedDependencies ×7、缺 LICENSE、SAST 未进 PR、无 fuzz、以及 6 个需组织级操作的检查项）。本卡把其中可由代码闭环的 13 个全部消除，其余 6 个中 2 个预期随合并后自动转绿（Maintained / CI-Tests），4 个（Branch-Protection / Code-Review / CII / 及以上残留）登记为需人类/组织动作并给出路径。

## 验收标准（逐条可执行）

1. `src/workbench/main.py` 中 `next` 只接受站内相对路径（`/` 开头且非 `//`），否则回落 `/items`；两处 `RedirectResponse(url=next)` 的污点源被切断（CodeQL #8/#9 关闭）。
2. 登录成功 Set-Cookie 的 value 由服务端 `HMAC(WORKBENCH_TOKEN, 常量)` 派生，用户输入不再流入 cookie 值；`require_session`/`optional_session` 改校验该派生值，既有 `tests/unit/test_workbench_pages.py` 全绿（CodeQL #7 关闭）。
3. `/health` 端点响应不再透出异常消息（仅 status + error_class），异常详情经 `logger.exception` 留在服务端日志；`tests/unit/test_cost_dashboard.py` 全绿（CodeQL #6 关闭）。
4. requirements*.txt 以 `uv pip compile --generate-hashes` 重锁（版本不变、仅加哈希），全部 workflow 与 Makefile setup 改用 `pip install --require-hashes`；`Cloudbird-Software/CI-Workflows` 复用工作流引用由 `@v1` 改钉 commit SHA（Scorecard #11–#17 关闭）。
5. SAST-on-PR（#19）：组织 2026-08-18 启用的 CodeQL default setup（actions+python，extended）已自动分析 PR 与 main push——本 PR 上 "Analyze (python)/(actions)" 检查即其产物；仓库不另加高级配置（与 default setup SARIF 互斥，GitHub 拒绝处理）。新增 `LICENSE`（Apache-2.0）（#20 关闭）；新增 `tools/fuzz/fuzz_pii_redaction.py` atheris harness（#18 关闭，W5-R 起由 Go 原生 fuzz 接棒进 gate）。
6. `make check` 本地全绿（沙箱无 docker 时以 CI 为准）；PR 合并后下一次 scorecard 扫描中：#6–#9（CodeQL）与 #11–#18（钉扎/哈希/fuzz）不再 open；#19/#20/#22/#24 预期随合并转绿；#10/#21/#23 属组织级动作（见 PR 描述），不在本卡宣称范围。
