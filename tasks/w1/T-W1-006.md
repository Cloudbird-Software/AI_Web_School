# 任务卡
```yaml
id: T-W1-006
wave: W1
title: 门证书模型与 gate_run/verdict 表
spec: [specs/constitution.md#D1-D2, specs/contracts/db/item-model.md#4.3, specs/constitution.md#A2]
context_paths: [specs/contracts/db/item-model.md, alembic/versions/]
deliverables: [DDL 迁移（gate_certificate + gate_run + gate_verdict 三表），src/core/gate/models.py，单元测试]
acceptance: make accept TASK=T-W1-006
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/gate
depends_on: [T-W1-001, T-W1-002]
non_goals: [校验器插件框架（W1 仅表结构），门策略矩阵实现，门编排/任务队列，抽检队列]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
创建校验域三本账表结构：`gate_certificate`（门证书，签发后只增不改）、`gate_run`（一次校验运行记录，含策略版本/验证器版本/证据/耗时/成本）、`gate_verdict`（每个验证器的判定结果）。替代 T-W0-005 中 `gate_certificate` 占位表。在 `src/core/gate/models.py` 提供 ORM 映射与 Pydantic schema。

## 验收标准（逐条可执行）
1. DDL 迁移创建三表，字段覆盖：`gate_certificate`（id/artifact_ref/cert_type/policy_version/issued_by/issued_at）、`gate_run`（id/certificate_id/policy_version/validator_id/validator_version/verdict{pass|fail|review}/evidence/confidence/cost_ms/cost_tokens/run_at）、`gate_verdict`（id/run_id/detail/created_at）
2. 三表均为只增不改（INSERT+SELECT，ORM 层无 update/delete 方法）
3. ORM 映射可正常插入与查询
4. 单元测试：(a) gate_certificate 插入后可回读；(b) gate_run 关联 certificate_id 正确；(c) gate 三表间 JOIN 查询正常
5. `make migrate-check` 全绿
