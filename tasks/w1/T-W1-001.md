# 任务卡
```yaml
id: T-W1-001
wave: W1
title: src/ 项目骨架与依赖落地（FastAPI + SQLAlchemy + Pydantic）
spec: [specs/constitution.md#第三部分]
context_paths: [Makefile, docker-compose.yml, requirements-dev.txt]
deliverables: [pyproject.toml, requirements.txt, src/__init__.py, src/core/__init__.py, 基础 conftest.py（含 DB fixture），make test 导通]
acceptance: make accept TASK=T-W1-001
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/
depends_on: [T-W0-005]
non_goals: [任何业务模型或 API 路由, pyproject.toml 以外的配置文件（docker-compose 等已在 W0 完成）]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
创建 `src/` Python 包骨架：`pyproject.toml`（含 FastAPI/SQLAlchemy[asyncio]/pydantic/asyncpg 依赖声明）、`requirements.txt`（锁定）、`src/core/__init__.py`。在 `tests/conftest.py` 中提供 PostgreSQL 测试 fixture（基于 `docker-compose` 中的 db 服务），确保 `make test` 可跑通一个连接测试。

## 验收标准（逐条可执行）
1. `pyproject.toml` 存在，声明 `fastapi`、`sqlalchemy[asyncio]`、`pydantic>=2`、`asyncpg` 等依赖
2. `requirements.txt` 由 `pip-compile` 或手工锁定生成，与 `pyproject.toml` 一致。若用 `pip-compile`，需将 `pip-tools` 加入 `requirements-dev.txt`（X8：新依赖须声明理由：用于锁定依赖版本）
3. `src/` 和 `src/core/` 目录存在 `__init__.py`
4. `tests/conftest.py` 提供 `async_session` fixture，可连接 PostgreSQL 并执行 `SELECT 1`
5. `make test` 全绿（含新建的 DB 连接烟测）
6. `requirements-dev.txt` 已存在内容不受影响（W0 既定依赖不丢失）
