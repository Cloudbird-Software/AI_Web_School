# 任务卡
```yaml
id: T-W1-002
wave: W1
title: 统一内容模型 DDL 迁移（按 L1 契约建 item 族全表）
spec: [specs/contracts/db/item-model.md, specs/constitution.md#D1-D3]
context_paths: [alembic/versions/, src/db/]
deliverables: [Alembic 迁移脚本（item 族 9 表：item/item_version/item_template/item_template_version/material/material_version/item_group/corpus_asset/corpus_version + item_kp + publication，含触发器），make migrate-check 全绿，DDL 与契约字段逐项对照测试]
acceptance: make accept TASK=T-W1-002
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/db
depends_on: [T-W1-001]
non_goals: [response_event 表（T-W1-005），gate_certificate/gate_run/gate_verdict 表（T-W1-006），学科包/学段包相关表，种子数据]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
按 L1 契约 specs/contracts/db/item-model.md（v1.1）创建新迁移，落地 item 族 9 张表：`item`、`item_version`、`item_template`、`item_template_version`、`material`、`material_version`、`item_group`、`corpus_asset`、`corpus_version`（契约 §2.4 素材与 §2.5 语料库均为身份+版本两段式，D1 全版本化）。同时建 `item_kp` 标注表与 `publication` 签发表。代替 T-W0-005 占位表——旧占位迁移不动，新迁移做结构替换。

## 验收标准（逐条可执行）
1. 新迁移 `upgrade()` 创建 item 族 9 表 + item_kp + publication，字段名/类型/约束与契约 §2 全部小节（2.1–2.5）一致
2. `item_version` 表含六大块字段（objective/interaction_ref/content/scoring_ref/error_bindings/lineage），均为 JSONB 或 text
3. `item_version` 状态机字段含 `status`（draft/quarantined/published/retired）+ `published_at` + `gate_certificate_id` + `retired_at`（契约 §2.2）
4. `make migrate-check`（upgrade→downgrade→upgrade）全绿
5. 单元测试：DDL 字段与契约逐项对照（至少覆盖 item/item_version/material_version 三个核心表的所有非 JSONB 列名+类型）
6. 旧占位迁移 `0001` 不做修改；新迁移的 downgrade 确实删除所有新建表
7. 循环外键（item↔item_version、material↔material_version、corpus_asset↔corpus_version）按契约 §6 注记处理（后加约束或 DEFERRABLE）
