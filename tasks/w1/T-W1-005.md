# 任务卡
```yaml
id: T-W1-005
wave: W1
title: 作答事件 append-only 写入服务
spec: [specs/contracts/events/response_event.md, specs/constitution.md#D1]
context_paths: [specs/contracts/events/, tests/contract/events/]
deliverables: [DDL 迁移（response_event 按月分区 + BEFORE UPDATE/DELETE 触发器 RAISE EXCEPTION），src/core/events/writer.py，单元测试]
acceptance: make accept TASK=T-W1-005
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/events
depends_on: [T-W1-001, T-W1-002]
non_goals: [评分执行逻辑，错误推断引擎，Parquet 归档管线，事件查询/聚合 API]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
按 L1 契约 [specs/contracts/events/response_event.md](file:///d:/开发项目/中小学教辅材料/specs/contracts/events/response_event.md) 创建 `response_event` 表的 DDL 迁移（按月分区，替代 T-W0-005 占位表）。迁移中加 `BEFORE UPDATE OR DELETE` 触发器直接 `RAISE EXCEPTION`——这是 D1"DB 权限禁 UPDATE/DELETE"的物理强制，不依赖角色体系。实现 `src/core/events/writer.py` 提供 `record_event()` 函数。

## 验收标准（逐条可执行）
1. DDL 迁移创建 `response_event` 表，字段与契约 §1 全要素一致（event_id/student_alias_id/item_version_id/scene/raw_payload/duration_ms/scoring_trace/error_inferences/testlet_id/session_id/audio_play_events/source_ref/created_at）
2. 按月分区（以 `created_at` 为分区键），至少创建初始分区；**主键含分区键：PK=(event_id, created_at)**（契约 §2 实现注记）
3. 迁移含 `BEFORE UPDATE OR DELETE ... FOR EACH STATEMENT EXECUTE FUNCTION raise_append_only_error()` 触发器
4. `record_event()` 函数签名接受契约 §1 所有必填字段（duration_ms/session_id 按契约 v1.1 为可空，NULL=未知/无会话），返回 `event_id`；内部仅执行 INSERT
5. 单元测试：(a) 写入一条事件后可 SELECT 回读；(b) `scene` 三值（practice/diagnosis/measurement）均可写入；(c) JSONB 字段（raw_payload/scoring_trace/error_inferences）可写入与回读结构一致；(d) 执行 UPDATE 或 DELETE 在 DB 层被拒绝（有失败测试）
6. `make migrate-check` 全绿（逆向迁移正确删除新表、分区与触发器函数）
