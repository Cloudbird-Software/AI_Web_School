# 任务卡
```yaml
id: T-W1-008
wave: W1
title: W1 出口脚本 + CI 集成（学科边界扫描 + 全链路烟测）
spec: [specs/constitution.md, specs/contracts/FROZEN.txt]
context_paths: [scripts/wave-exit/, .github/workflows/, tests/]
deliverables: [scripts/wave-exit/w1.sh，.github/workflows 更新（如需要），w1 出口全绿]
acceptance: make accept TASK=T-W1-008
accept_script: scripts/wave-exit/w1.sh
model_floor: T1
token_budget: 400k
owner_module: scripts/wave-exit
depends_on: [T-W1-002, T-W1-003, T-W1-004, T-W1-005, T-W1-006, T-W1-007]
non_goals: [新增 CI workflow 类型，修改 W0 出口项，生产环境部署配置]
escalation: fail×2 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
编写 `scripts/wave-exit/w1.sh`，定义 W1 出口检查项：确认所有 W1 交付物存在、学科边界扫描（核心域无学科包 import）、`make migrate-check` 全绿、全量测试套件绿、冻结契约仍处于冻结状态。如有必要更新 CI nightly workflow 加入 W1 特有检查。本卡也是 W1 的集成验收点。

## 验收标准（逐条可执行）
1. `scripts/wave-exit/w1.sh` 存在且可执行
2. 出口脚本至少检查：(a) `src/core/models/`、`src/core/registries/`、`src/core/events/`、`src/core/gate/`、`src/core/content/` 包存在；(b) `make migrate-check` 全绿；(c) `make test` 全绿；(d) 核心域无学科包 import——使用 W0 pr-check 同款模式 `(import|from)\s+(packs|subject_)` 扫描 `src/core/` 下无匹配；(e) 冻结契约清单 `FROZEN.txt` 行数未减少
3. 执行 `bash scripts/wave-exit/w1.sh` 全绿
4. CI nightly workflow 如需要新增步骤，已在 PR 中更新并说明理由
5. 本卡作为 W1 最后一张卡，依赖项全部完成后才可验收通过
